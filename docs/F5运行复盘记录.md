# F5 运行复盘记录

本文档用于记录每一次 F5 / 实机运行后的证据、结论与改进项。原则是：以日志和实际界面观察为准，区分已确认问题、用户观察、待验证假设，避免把一次运行中的外部服务失败误判成核心架构失败。

## 记录规范

每次运行后追加一条记录，至少包含：

- 运行时间与日志文件
- 本次验证目标
- 关键现象
- 已确认问题
- 待验证假设
- 下一步改进项
- 是否阻断终极计划验收

问题优先级建议：

- P0：阻断模型对话、Seed Plan、批次生成、动态介入主链路
- P1：不阻断主链路，但明显影响演示稳定性、交互理解或用户信心
- P2：体验、可观测性、边缘能力或后续工程质量改进

## 2026-06-18 15:52 最新运行复盘

### 基本信息

- 日志文件：`E:\corona\CoronaEngine\build\examples\engine\RelWithDebInfo\logs\2026-06-18_15-52-01_corona.log`
- 日志大小：约 92 KB
- 运行结束时间：2026-06-18 15:56:28
- 本次观察目标：检查最新一次运行中的模型连接失败，并记录“当前界面对单人不太友好”的问题，作为后续改进依据。

### 运行链路摘要

本次运行中，引擎和 AI 插件能够启动，AITalkBar 页面被加载：

- `15:52:17`：`cai_extensions` 命令注册完成。
- `15:53:45`：加载 `/AITalkBar?standalone=1`。
- `15:54:42`：LANChat Agent 收到用户触发：`为我介绍一下大家`，角色为 `长者`。
- `15:56:08`：LANChat Agent 收到用户触发：`你好`，角色为 `GM`。

但两次进入 integrated stream 后，模型调用均在连接层失败，无法稳定获得模型响应。

### 已确认问题

#### P0：模型连接失败，阻断 Agent 对话与后续生成链路

证据等级：[日志暴露的实际问题] / [已确认]

关键日志：

- `15:54:47`：`LLM 意图分类失败, 回退关键词: Connection error.`
- `15:54:51`：`模型调用失败 (尝试 1/3): Connection error.`
- `15:54:53`：`模型调用失败 (尝试 2/3): Connection error.`
- `15:54:57`：`连接错误，重建客户端重试 (1/1): Connection error.`
- `15:55:10`：`处理 integrated stream 入口异常: Connection error.`
- `15:55:10`：`httpcore.ConnectError: [SSL: UNEXPECTED_EOF_WHILE_READING] EOF occurred in violation of protocol`
- `15:55:10`：`openai.APIConnectionError: Connection error.`
- `15:56:28`：第二次请求再次出现 `openai.APIConnectionError: Connection error.`

判断：

这次失败的直接原因是模型服务连接层异常，发生在 httpx / httpcore / OpenAI client 调用 `/chat/completions` 阶段。更像是代理、网络、TLS、base_url、证书链或模型服务可达性问题，不是当前多人 / 多 Agent 批次协议本身先崩。

影响：

- 阻断 GM / Agent 正常回复。
- 阻断模糊意图确认、Seed Plan 生成、批次生成触发。
- 会让用户误以为系统整体无响应或 Agent 能力失败。

下一步改进：

- 增加模型连接预检：进入聊天室或触发 Agent 前，先做轻量模型健康检查。
- 增加前端友好错误态：把 `Connection error` 显示成“模型服务连接失败，请检查网络/代理/模型配置”，不要只让用户等待。
- 在运行复盘中单独区分“外部模型不可达”和“架构链路失败”。
- F5 验收前增加一项必查：确认当前模型 provider、base_url、proxy、API key 可用。
- 2026-06-18 补充：用户已确认网络/代理问题已处理，下一轮 F5 应重点验证 integrated stream 是否能稳定拿到模型响应；若仍失败，再回到 base_url、TLS、provider 配置和 Python 运行环境排查。

#### P1：单人界面对当前验证目标不够友好

证据等级：[用户观察] / [待补日志或截图验证]

现象：

用户反馈“当前界面对于单人来说不太友好”。结合当前目标，现阶段 UI 更偏多人房间 / 多 Agent 调试入口，对于单人验证者来说，可能存在以下体验成本：

- 单人用户仍要理解房主、参与者、Agent、GM 等多人概念。
- 从“我想生成一个场景”到“进入意图确认 / Seed Plan / 生成批次”的路径不够短。
- 模型连接失败时，界面可能缺少明确的外部服务失败提示。
- 进度信息需要适当暴露，但不能泄露内部链路细节；当前还需要继续校准展示层级。

判断：

这不是终极计划主链路的协议阻断项，但会明显影响单人 F5 验收和演示体验。对于 UbiComp 原型而言，不必做产品级完整 UI，但至少需要一个“单人快速验证路径”。

下一步改进：

- 增加或整理单人验证模式：
  - 默认用户就是房主。
  - 默认展示一个 GM 和必要 Agent。
  - 提供清晰的“描述需求 -> 确认方案 -> 开始生成 -> 批次介入”的线性入口。
  - 多人权限、房间细节、内部调度细节默认收起。
- 增加阶段级进度展示：
  - 意图确认中
  - 方案整理中
  - 等待确认
  - 第 N 批生成中
  - 可介入
  - VLM 审查中
  - 最终整理中
- 错误态要面向用户，不暴露 Python 堆栈、provider 内部类名或工具链细节。

#### P1：部分媒体 / 3D 工具配置缺失

证据等级：[日志暴露的实际问题]

关键日志：

- `图像生成不可用：未配置账号池且旧客户端配置不完整`
- `Failed to load tools from tools.model_tools: Rodin api_key 缺失`
- `混元3D 已禁用 (enable=False)，跳过工具加载`
- `视频生成不可用：未配置账号池且旧客户端配置不完整`

判断：

这说明当前运行环境并不是完整媒体生成环境。若本次 F5 验收目标包含真实图像、视频、Rodin 或 Hunyuan 3D 生成，需要先明确启用哪些 provider；若只验证多人 / 多 Agent / Seed Plan / 批次协议，可以先降级为 mock 或轻量工具路径。

影响：

- 会影响“真实场景生成质量”验证。
- 不一定影响“聊天室意图确认、Seed Plan、批次状态流转、动态介入协议”验证。

下一步改进：

- F5 前明确本轮验收是“协议链路验证”还是“真实生成验证”。
- 对未配置 provider 的能力，在 UI 和日志中显示为“未启用”，避免被误判成系统异常。
- 若要验证真实 3D 生成，提前配置 Rodin / Hunyuan / VLM 所需 key 与开关。

#### P2：Workflow sync scope 降级

证据等级：[日志暴露的实际问题]

关键日志：

- `workflow sync scope unavailable (Quasar 版本缺 register_workflow_execution_scope_factory)，已降级跳过`

判断：

当前 Quasar 运行版本缺少 `register_workflow_execution_scope_factory`，导致 workflow sync scope 只能降级跳过。这不一定是本次模型失败的根因，但会影响 WorkflowState 与底层工作流执行作用域之间的强绑定能力。

下一步改进：

- 核对源码版本与运行目录中的 Quasar 是否一致。
- 如果终极计划要求 WorkflowState 与生成执行强一致，需要补齐该 factory 或改成兼容适配层。
- 该项需要继续通过 Python 层静态核对和 F5 实机行为验证。

#### P2：运行环境存在非阻断告警

证据等级：[日志暴露的实际问题]

关键日志：

- `Skipping device 'AMD Radeon 780M Graphics' due to Vulkan API 1.3.246 (< 1.4)`
- `Scene '场景1': 加载 actor 'ball' 失败，已跳过：'utf-8' codec can't decode byte 0xb4`
- `scene_breakdown tools not available (skipped)`

判断：

这些告警目前不是模型连接失败的直接原因，但会影响实机演示稳定性和场景加载完整性。尤其 actor 编码问题如果出现在演示场景资产中，需要单独处理。

### 本次结论

本次运行不能用于判定“终极计划失败”，因为主阻断点是模型连接层失败，而不是批次生成 / 动态介入 / GM 仲裁协议先失败。

但它暴露出两个必须纳入后续改进的真实问题：

1. 模型连接失败需要前置预检和用户友好错误态，否则每次 F5 都可能被外部服务问题打断。
2. 单人验证路径需要简化，否则即使底层链路可用，验证者也容易在房间、角色、Agent、GM、Seed Plan 之间迷路。

### 后续任务建议

| 优先级 | 任务 | 验证方式 | 是否阻断终极计划验收 |
| --- | --- | --- | --- |
| P0 | 增加模型 provider 预检与失败提示 | Python 轻量测试 + F5 实机验证 | 是 |
| P0 | F5 前检查 base_url / proxy / API key / TLS 可达性 | 手工预检 + 日志确认 | 是 |
| P1 | 增加单人快速验证路径或单人模式说明态 | 前端轻量测试 + F5 实机验证 | 不一定，但强烈建议 |
| P1 | 未配置媒体/3D provider 时显示“未启用”而不是异常感 | Python/前端逻辑验证 | 不阻断协议验收，阻断真实生成验收 |
| P2 | 修复或兼容 workflow sync scope factory 缺失 | Python 静态核对 + F5 实机验证 | 视 WorkflowState 强一致目标而定 |
| P2 | actor 编码问题单独排查 | 静态资产检查 + F5 实机验证 | 不阻断 Agent 链路，可能影响场景展示 |

## 追加模板

### YYYY-MM-DD HH:mm 运行复盘

#### 基本信息

- 日志文件：
- 本次验证目标：
- 操作路径：
- 是否使用真实模型：
- 是否使用真实 3D / VLM provider：

#### 运行链路摘要

-

#### 已确认问题

#### 待验证假设

#### 本次结论

#### 后续任务建议

## 2026-06-18 聊天室交互复盘：模型恢复后执行未闭环

### 基本信息

- 日志文件：日志目录最新落盘文件仍为 `E:\corona\CoronaEngine\build\examples\engine\RelWithDebInfo\logs\2026-06-18_15-52-01_corona.log`，未发现比它更新的 `*_corona.log`。
- 本次证据来源：用户提供的聊天室 transcript。
- 本次验证目标：确认网络/代理恢复后，聊天室中的 Agent、GM、房主确认、方案执行是否形成闭环。

### 聊天链路摘要

本次 transcript 显示模型回复已经恢复：

- 房主 `@小女孩 你好呀` 后，小女孩正常回复。
- 房主提出场景目标：“有点神秘感的室外集市，不要太恐怖，适合几个人逛。”
- 房主补充风格：“更温暖一点，有灯光和休息区，不要全是暗黑风。”
- 房主要求 `@GM 先整理一下大家的意图，不要马上生成。`
- GM 能输出总结，但认为“还没有足够明确的多人共识”。
- 房主对小女孩给出明确执行目标：“做一个夜晚幻想集市，有入口、摊位、灯光、小休息区，整体风格统一。”
- GM 生成 `gm-1781769861320`、`gm-1781769914868` 两个提案，房主均确认。
- 确认后未看到 Seed Plan、生成任务、资源调度、批次生成或执行开始的明确反馈。
- 房主追问“为什么不执行呀”时，该追问又被 GM 生成新的提案 `gm-1781769941559`，而不是解释当前执行状态或触发生成。
- 拒绝 `gm-1781769941559` 后，界面仍保留“确认/拒绝”按钮；再次确认时，GM 提示“已处理 rejected，不会重复入队”。

### 已确认问题

#### P0：确认 GM 提案后没有进入 Seed Plan / 执行闭环

证据等级：[用户 transcript 暴露的实际问题] / [待日志补证]

现象：

- 房主两次确认 GM 提案后，只得到“已确认 gm-...”。
- transcript 中没有出现类似“方案已确认”“开始生成”“第 1 批生成中”“资源调度中”“Seed Plan confirmed/executing”的用户可见反馈。
- 房主不得不追问“为什么不执行呀”。

判断：

当前主链路很可能卡在 GM conflict proposal confirmation 与 Seed Plan / GenerationScheduler submit 之间。也就是说，系统已经能做“提案-确认”的表层交互，但没有把确认语义稳定转成“构造 Seed Plan 并进入执行”的控制面动作。

可能根因：

- GM 提案类型被当成通用冲突提案，而不是“可执行 Seed Plan 草案”。
- `@小女孩 做一个...` 这种面向 Agent 的执行请求被 GM 仲裁层截获，生成了冲突确认，而没有进入 Seed Plan builder。
- 确认 `gm-...` 后只更新 proposal 状态，没有调用 plan confirmation / scheduler submit。
- 当前确认动作缺少明确 `proposal_type` / `intent_type`，导致后端无法区分“讨论冲突确认”和“执行方案确认”。

影响：

- 直接阻断终极计划中的“最终方案确认 -> 场景生成”。
- 多人/多 Agent 讨论能够启动，但无法自然过渡到批次生成。
- 用户体验上会表现为“系统一直让确认，但确认完不干活”。

下一步改进：

- GM proposal 必须区分 `clarification_proposal`、`conflict_resolution`、`seed_plan_proposal`、`execution_confirmation`。
- 对“明确生成场景”的房主请求，应优先生成 Seed Plan 草案，而不是泛化成冲突提案。
- 房主确认 `seed_plan_proposal` 后，必须进入 `SeedPlan.confirmed -> scheduler submit -> stage=资源调度/生成中`。
- 如果当前能力暂不执行真实生成，UI 必须明确说明“已确认方案，但真实生成 provider 未启用/待执行”，不能静默。

#### P0：执行意图被错误包装为“可能影响多人共识”的通用冲突提案

证据等级：[用户 transcript 暴露的实际问题]

关键表现：

- 对“夜晚幻想集市，有入口、摊位、灯光、小休息区，整体风格统一”这种完整生成意图，GM 输出的是：
  - “潜在冲突：暂无明确对象冲突，但该操作可能影响多人共识或核心布局。”
  - “建议：先保留最近用户明确操作，Agent 物体让位...”
- 这套话术适合介入已有生成或多人冲突处理，不适合首次执行方案确认。

判断：

当前 GM 仲裁策略过宽，把普通生成请求也套进“冲突/覆盖多人意见”的流程，导致执行入口被确认流程吞掉。对单人或房主明确主导的场景，这会制造不必要的确认负担。

下一步改进：

- 增加 intent gate：
  - 初始阶段 + 房主明确生成目标 -> `seed_plan_proposal`
  - 已有执行中 + 修改/删除/覆盖/冲突 -> `intervention_or_conflict`
  - 非房主含糊建议 -> `discussion_note`
- 单人/房主主导模式下，初始生成不应触发“影响多人共识”式冲突确认，除非确有其他用户意见冲突。

#### P1：系统进度披露内容明显错位且重复

证据等级：[用户 transcript 暴露的实际问题]

现象：

- 多次出现“系统：有一项仅房主可见的进度更新。”
- 随后又出现“系统：讨论中：哈喽，大家好”。
- 该文案与当前真实对话不匹配：用户已经在讨论“夜晚幻想集市”，但系统仍显示“哈喽，大家好”。

判断：

DisclosurePolicy / 前端 disclosure 展示层可能在重复展示旧的、默认的或错误归属的进度消息。当前阶段披露没有起到缓解用户焦虑的作用，反而让人觉得系统状态混乱。

下一步改进：

- “仅房主可见”的系统提示不应作为普通聊天消息反复刷屏；应进入阶段条或房主侧状态区域。
- `讨论中：哈喽，大家好` 这类 disclosure 必须绑定最新有效 user intent，不应复用旧 trigger。
- 如果有 host-only disclosure，参与者侧只应看到安全摘要；房主侧应看到具体待确认项，不要出现占位式笼统提示。

#### P1：已拒绝提案的确认按钮仍残留

证据等级：[用户 transcript 暴露的实际问题]

现象：

- 房主拒绝 `gm-1781769941559` 后，界面仍显示“确认 / 拒绝”按钮。
- 用户再次确认该提案，GM 回复“提案已处理 rejected，不会重复入队。”

判断：

后端具备幂等防护，没有重复入队，这是正确的；但前端本地 disclosure dismiss / confirmed-rejected 状态同步不完整，导致用户还可以点击已失效动作。

下一步改进：

- 拒绝或确认后，前端应立即 dismiss 对应 `proposal_id` 的按钮。
- 历史消息中的按钮若无法删除，也应 disabled 并显示“已拒绝/已确认”。
- 后端幂等提示保留，但不应成为常态交互路径。

#### P1：单人/房主主导模式交互仍然过重

证据等级：[用户观察 + transcript 佐证]

现象：

- 单人房主连续表达明确目标，系统仍要求多轮 GM 提案确认。
- 用户显式说“先整理，不要马上生成”后，后续又明确要求执行，但系统没有清晰地区分“整理阶段”和“执行阶段”。

判断：

当前流程更像多人冲突保护模式，没有给单人/房主主导验证路径做轻量化。对于 UbiComp 原型，这会严重拖慢演示节奏。

下一步改进：

- 增加 `solo_or_host_led` 模式判断。
- 房主明确说“按照...做/开始/执行/生成”时，应从讨论态切到方案确认或执行态。
- 如果系统还需要确认，应只确认一次 Seed Plan，而不是每条执行句都生成 GM conflict proposal。

### 本次结论

这次运行比上一轮更有价值：网络/模型层看起来已经恢复，Agent 能正常回复；真正暴露的是控制面闭环问题。

核心瓶颈不是“模型不聪明”，而是：

1. 执行意图没有稳定路由到 Seed Plan。
2. GM 提案类型过于泛化，把初始生成请求当冲突处理。
3. 房主确认后没有触发可见的生成状态转移。
4. UI disclosure 把内部阶段消息变成了重复、错位的聊天噪音。
5. 前端确认按钮状态同步还不够干净。

### 后续任务建议

| 优先级 | 任务 | 验证方式 | 是否阻断终极计划验收 |
| --- | --- | --- | --- |
| P0 | 给 GM proposal 增加类型区分：`seed_plan_proposal` 与 `conflict_resolution` 分离 | Python Coordinator 测试 | 是 |
| P0 | 房主确认 `seed_plan_proposal` 后必须进入 Seed Plan confirmed / scheduler submit / stage disclosure | Python Worker + Coordinator 测试，F5 验证 | 是 |
| P0 | 修正初始生成意图路由：房主明确生成目标不应默认走冲突提案 | Python intent/Coordinator 测试 | 是 |
| P1 | disclosure 展示从聊天刷屏改为阶段条/状态区，避免重复“仅房主可见”消息 | 前端轻量测试 + F5 验证 | 否，但影响演示 |
| P1 | 确认/拒绝后前端立即清理或禁用对应按钮 | 前端轻量测试 + F5 验证 | 否，但影响可信度 |
| P1 | 单人/房主主导模式减少重复确认 | Python + 前端轻量测试 | 否，但强烈建议 |

## 2026-06-18 后续聊天室复盘：进入生成队列但没有真实生成

### 基本信息

- 日志文件：`E:\corona\CoronaEngine\build\examples\engine\RelWithDebInfo\logs\2026-06-18_15-52-01_corona.log`
- 证据来源：用户提供的聊天室 transcript、UI 截图、日志关键词检索。
- UI 截图现象：顶部显示 `生成中 0%`，消息区显示 `SeedPlan seed-319be7ebdbf6 已进入生成队列：gen-6990b6a8a376 (queued)`。
- 日志核对：当前落盘日志能看到 16:00、16:02、16:09 的 Agent 普通聊天 stream 完成，但未检索到 `seed-4284efb2a928`、`seed-1571e97d7c6a`、`seed-319be7ebdbf6`、`gen-846ffb677d21`、`gen-d567b1e0bf19`、`gen-6990b6a8a376` 等生成 ID，也没有看到对应 `GenerationScheduler` / `progressive` / `SceneSession` / `batch` 推进日志。

### 运行链路摘要

相较上一条复盘，本次链路有进展：

- 房主确认 `gm-1781770271877` 后，GM 输出 `已排队 / 执行中 / 已完成`，并提示 `SeedPlan seed-4284efb2a928 已进入生成队列：gen-846ffb677d21 (queued)`。
- 房主询问“现在的生成计划是什么”后，又触发新 SeedPlan：`seed-1571e97d7c6a -> gen-d567b1e0bf19 (queued)`。
- UI 截图中还出现：`seed-319be7ebdbf6 -> gen-6990b6a8a376 (queued)`。
- 系统能够进入“可介入窗口”，并记录“一个天使雕像”“天使雕像要足够大”等动态介入。

但用户从终端观察“没有进入生成”，且落盘日志没有 batch start / running / done 等证据。

### 已确认问题

#### P0：SeedPlan 已进入生成队列，但没有从 queued 推进到真实 batch 生成

证据等级：[用户 transcript 暴露的实际问题] / [UI 截图确认] / [日志缺失佐证]

现象：

- UI/GM 明确宣称多个 SeedPlan 已进入生成队列，状态为 `(queued)`。
- UI 顶部显示 `生成中 0%`，进度条停在 0%。
- 用户从终端观察没有进入真实生成。
- 落盘日志没有对应 `seed-*` / `gen-*` ID，也没有 `GenerationScheduler running`、`progressive compose`、`batch start` 等生成推进证据。

判断：

当前已经不是“确认后完全不动”，而是更深一层：控制面能够把 SeedPlan 包装成 queued 状态并反馈给 UI，但生成 worker / scheduler 消费链路没有真正启动，或启动日志没有接入当前 `corona.log`。

可能根因：

- `GenerationScheduler.submit()` 返回 queued，但 worker loop 没有被唤醒或没有配置真实 composer。
- queued job 被放入调度器，但当前资源状态、暂停状态或 room/session 锁导致一直不出队。
- Worker 只发了“进入队列”的 optimistic UI disclosure，没有确认 scheduler 实际进入 running。
- 生成执行入口仍是 mock / placeholder，返回 queued 后没有绑定真实 `SceneSession.progressive_compose()`。
- 生成链路日志没有统一落盘，导致 UI 有状态但日志不可诊断。

下一步改进：

- 给 scheduler job 增加强制状态回流：queued -> running -> batch_started -> batch_done / failed。
- Worker 发出 `生成中 0%` 前，必须确认 job 已被真实 scheduler 接管；否则显示“已排队，等待资源”。
- 增加 queued 超时诊断：超过 N 秒未进入 running，输出 `no_worker`、`paused`、`composer_missing`、`queue_blocked`、`provider_disabled` 等原因。
- 所有 `seed_id` / `generation_id` 必须进入落盘日志，便于从 UI 反查后端状态。

#### P0：查询/解释类消息被错误转成新的生成计划

证据等级：[用户 transcript 暴露的实际问题]

现象：

- 房主问 `@GM 为什么执行生成计划`，系统将其转成执行任务并生成 `SeedPlan seed-4284efb2a928`。
- 房主问 `@小女孩 现在的生成计划是什么`，系统再次生成 `SeedPlan seed-1571e97d7c6a`。

判断：

意图分类 / GM proposal gate 仍然过宽。解释型、状态查询型消息应该路由到 status query / explanation，而不是生成新的 SeedPlan。否则用户每问一次“现在怎样”，系统就多排一个生成任务，最终导致队列污染、资源溢出和方案漂移。

下一步改进：

- 增加 `status_query` / `explain_status` intent。
- 当文本包含“为什么不执行 / 现在计划是什么 / 当前情况 / 进度怎样”时，优先查询已有 plan/job 状态。
- 已有 queued/running SeedPlan 时，默认不新建 SeedPlan，除非房主明确说“重新生成 / 新建方案 / 覆盖当前方案”。

#### P0：同一目标被重复创建多个 SeedPlan / generation job

证据等级：[用户 transcript 暴露的实际问题] / [UI 截图确认]

现象：

- 同一“夜晚幻想集市”目标附近至少出现多个 SeedPlan / generation job：
  - `seed-4284efb2a928 -> gen-846ffb677d21`
  - `seed-1571e97d7c6a -> gen-d567b1e0bf19`
  - `seed-319be7ebdbf6 -> gen-6990b6a8a376`
- 它们都表现为 queued / 0%，未看到真实完成。

判断：

当前缺少 active plan guard。当一个 room 已经有 queued/running SeedPlan 时，后续查询、解释、补充和小调整应进入同一个 active plan 的 intervention/memory，而不是创建新 plan/job。

下一步改进：

- `InteractionCoordinator` 增加每 room active execution guard。
- queued/running/paused 状态下禁止重复 submit 同目标 SeedPlan。
- query/status 类消息只读 active plan/job。
- add/modify 类消息进入 active plan pending intervention。

#### P1：UI 把 queued 显示成“生成中 0%”，误导用户

证据等级：[UI 截图确认] / [用户观察]

现象：

- 截图中 job 状态仍为 `(queued)`，但顶部显示 `生成中` 与 `生成中 0%`。
- 这会让用户以为生成已经开始，只是进度慢。

判断：

UI disclosure 对 scheduler 状态的映射过粗。queued、waiting_resource、running、batch_generating、paused、failed 应分别展示。

下一步改进：

- queued：显示“已排队，等待生成资源”。
- running/batch_started：才显示“生成中”。
- queued 超时：显示“仍在排队，可能是资源或执行器未启动”。
- failed：显示可行动错误，不要停在 0%。

#### P1：日志与 UI 状态不可对账

证据等级：[日志缺失佐证]

现象：

- UI 中出现多个 `seed-* / gen-*`，但当前落盘日志没有对应 ID。
- 日志能看到普通 Agent stream 完成，如 `integrated stream 请求处理完成`，但看不到生成任务状态机。

判断：

当前 Observability 不足。即使后端真的创建了 queued job，缺少统一落盘会导致无法判断卡在 submit、scheduler queue、worker loop、composer factory、provider，还是 UI 状态映射。

下一步改进：

- 所有 SeedPlan confirm / scheduler submit / status change / batch event 必须 `INFO` 落盘。
- 每条日志带 `room_id / seed_id / generation_id / stage / status / reason`。
- UI disclosure 的 `generation_id` 必须能在日志中搜到同 ID。

### 本次结论

这次运行说明系统已经越过上一阶段的“确认后完全不动”，但新的阻断点更具体：

1. SeedPlan 能进入 queued。
2. UI 能显示生成状态。
3. 动态介入能被记录。
4. 但 generation job 没有推进到 running / batch_started。
5. 查询类消息还会误触发新 SeedPlan，造成重复排队。

因此下一步最高优先级不是继续优化 GM 文案，而是修 scheduler/worker 执行闭环与 active plan guard。

### 后续任务建议

| 优先级 | 任务 | 验证方式 | 是否阻断终极计划验收 |
| --- | --- | --- | --- |
| P0 | 补 SeedPlan/generation_id 全链路日志：confirm、submit、queued、running、batch_started、failed | Python 测试 + F5 日志核对 | 是 |
| P0 | 查明 queued 不出队原因：worker loop、composer_factory、scheduler pause/resource gate | Python 层测试 + 静态核对 + F5 | 是 |
| P0 | 增加 active plan guard，防止状态查询创建新 SeedPlan | Coordinator/Worker 测试 | 是 |
| P0 | 增加 status_query intent，让“现在计划是什么/为什么不执行”查询状态而非新建生成 | Coordinator/Agent adapter 测试 | 是 |
| P1 | UI 区分 queued 与 running，queued 不显示“生成中” | 前端轻量测试 + F5 | 否，但强烈影响体验 |
| P1 | queued 超时显示可诊断原因 | Python/前端协议测试 | 否，但强烈建议 |

## 2026-06-18 新房间复盘：已进入真实生成，但卡在资源准备

### 基本信息

- 日志文件：`E:\corona\CoronaEngine\build\examples\engine\RelWithDebInfo\logs\2026-06-18_15-52-01_corona.log`
- 运行阶段：新建房间后，房主通过 `@商人 确认开始` 触发真实 compose。
- 本次结论：系统已经进入真实场景组合，不再只是 queued；主要慢点在 `model_retrieval workflow` 的文生图补偿与后续资源准备。

### 关键时间线

- `16:20:44`：旧房间 session stopped。
- `16:20:46`：新房间 session active，host 监听端口 8770。
- `16:22:14`：商人收到初始需求：“神秘感的室外集市，不要太恐怖，适合几个人逛。”
- `16:22:14`：路由到 `planning confirmation gate`。
- `16:22:40`：商人收到补充：“更温暖一点，有灯光和休息区，不要全是暗黑风。”
- `16:22:44`：LLM 意图分类为 `edit`，路由到 edit，因此 UI 返回“场景里没有可编辑的物体”。
- `16:23:59`：`确认开始` 被路由到 `compose (confirmed plan)`。
- `16:24:00`：`SceneComposer` 开始三阶段场景组合。
- `16:24:14`：提取到 9 个物体：`摊位、入口拱门、导视牌、灯笼、展示架、长桌、绿植、地毯、货箱`。
- `16:24:33`：场景分解为 1 个 Zone：`神秘室外集市活动区(terrain)`。
- `16:24:33`：物体数 9 超过上限 8，截断为 8。
- `16:24:33`：调用原 `model_retrieval workflow`，8 个物体走“文生图 -> 图生 3D”。
- `16:24:33`：8 个元素无生成图片，进入补偿重试。
- `16:24:33`：`TaskExecutor` 初始化，线程池大小 16。
- `16:27:28`：第一张补偿图片探针成功：`摊位`，耗时约 2 分 55 秒。
- `16:27:28`：开始并发补偿图片：`items=7 workers=3`。
- `16:30:11`：`导视牌` 补偿图片成功。
- `16:30:17`：`灯笼` 补偿图片成功。
- `16:30:29`：`入口拱门` 补偿图片成功。

### 已确认问题

#### P0：资源准备链路太慢，主要卡在文生图补偿

证据等级：[日志暴露的实际问题]

现象：

- 从 `16:24:33` 进入 `model_retrieval workflow` 到 `16:27:28` 第一张补偿图片探针成功，约 175 秒。
- 后续并发补偿 7 个元素，3 个 worker，到 `16:30:29` 只明确看到 3 个元素成功。
- UI 侧对应“生成进度 32%：准备所需模型，正在获取主体和物件资源，界面可能需要等待一会儿。”

判断：

这次慢不是模型聊天慢，也不是 queued 不出队；系统已经进入真实生成，但资源准备阶段极慢。当前逻辑在没有现成图片/模型资源时，对 8 个物体触发文生图补偿，再接图生 3D，这天然耗时高。

下一步改进：

- 对 UbiComp demo，优先走“可复用模型库 / 轻量占位模型 / 低成本检索”，不要每个物体都实时文生图再图生 3D。
- 对装饰类小物体降低实时生成优先级：先用通用摊位、灯笼、桌椅、植物等资产占位。
- 把资源准备拆成批次：第一批先生成入口、摊位、灯光等 3-4 个关键对象，其余对象后续补。
- UI 要明确显示“正在准备模型资源，预计较慢”，而不是只停在 32%。

#### P0：资源调度仍绕过统一 scheduler，存在并发与资源溢出风险

证据等级：[日志暴露的实际问题]

关键日志：

- `TaskExecutor 初始化完成，线程池大小: 16`
- `并发补偿图片: items=7 workers=3`
- `ThreadPoolExecutor-14_*`

判断：

本轮实际生成仍然走到了 Quasar 旧 `TaskExecutor` 与 workflow 内部 `ThreadPoolExecutor`。这说明终极计划中的资源调度收口还没有覆盖这条 `model_retrieval workflow` 的图片补偿路径。

影响：

- 多人/多 Agent 场景中，多个生成任务同时触发时，旧线程池可能继续绕过 `GenerationScheduler` 背压。
- UI 只能看到笼统进度，不能看到资源排队、每个物体耗时、失败原因。
- 用户动态介入虽然被记录，但资源准备长时间占用期间，后续批次吸收时机不可控。

下一步改进：

- 把 `model_retrieval_workflow.dispatch` 的补偿图片任务接入统一 scheduler 或至少接入限流适配器。
- 每个资源任务带 `room_id / plan_id / batch_id / object_name / stage`，便于取消和复盘。
- 默认并发从旧线程池 16 收敛到可配置的小并发，并根据 provider 限额做 backpressure。

#### P1：生成前偏好补充被误判为 edit，导致“场景里没有可编辑物体”

证据等级：[用户 transcript + 日志确认]

现象：

- 房主说“我希望它更温暖一点，有灯光和休息区，不要全是暗黑风。”
- 日志显示 LLM 意图分类为 `edit`，路由到 `edit (意图分类，专用变换执行)`。
- UI 返回：“场景里没有可编辑的物体。”

判断：

在 Seed Plan 尚未确认、场景尚未生成前，这句话应作为 plan preference / requirement refinement，而不是 scene edit。当前意图分类没有结合阶段状态。

下一步改进：

- 若当前 room 没有 active generated scene / editable actor，则 `edit` 类文本应降级为 plan refinement。
- 只有存在可编辑 actor 或明确目标 actor 时，才进入 edit 执行。
- UI 不应直接返回“没有可编辑物体”，应提示“已作为方案补充记录”。

#### P1：否定偏好被抽取反向，`不要太恐怖` 变成了“太恐怖”

证据等级：[用户 transcript 暴露的实际问题]

现象：

- 用户说“不要太恐怖”。
- 系统披露：“可介入窗口：已记录‘太恐怖’”。

判断：

信息提炼丢失了否定词，导致用户偏好语义反转。对于风格控制，这是高风险问题：如果后续进入生成，可能把“不要恐怖”理解成“恐怖”。

下一步改进：

- 信息提炼必须保留 negation，如 `不要/别/不希望/避免/不要全是`。
- 风格约束应结构化为 `avoid: ["太恐怖", "全暗黑风"]`，而不是自由文本截断。
- UI 披露应显示“已记录：避免太恐怖”，避免用户误解。

#### P1：动态介入能记录，但真实吸收时机仍待验证

证据等级：[用户 transcript 暴露的实际问题] / [待后续日志验证]

现象：

- `调整：再加一个天使雕像` 被记录为下一批/最终调整。
- `新增：再增加一只小狗` 被记录为下一批/最终调整。
- 但日志当前主要停留在资源准备阶段，尚未看到后续 batch boundary 或最终调整吸收这些介入。

判断：

动态介入入口已经可用，但资源准备耗时过长时，用户介入到底何时进入 batch planning、资源请求或最终调整，还需要继续观察。

下一步改进：

- 在资源准备阶段也要回显 pending intervention 队列摘要。
- 当下一批开始前，明确输出“已吸收：天使雕像 / 小狗”。
- 若介入发生在资源准备中，应允许影响尚未提交的资源列表，而不是等最终调整才补。

### 本次结论

本轮是明显进展：系统已从 `queued` 进入真实 `SceneComposer` 与 `model_retrieval workflow`。但暴露的新主瓶颈是资源准备耗时与调度绕行。

对终极计划而言，下一步优先级应调整为：

1. 将资源准备路径纳入统一 scheduler / 限流 / 可观测。
2. 降低 demo 场景对实时文生图 + 图生 3D 的依赖，优先复用资产或分批生成关键对象。
3. 修复生成前补充被误判为 edit 的阶段感知问题。
4. 修复否定偏好丢失，避免风格意图反转。

### 后续任务建议

| 优先级 | 任务 | 验证方式 | 是否阻断终极计划验收 |
| --- | --- | --- | --- |
| P0 | 给 `model_retrieval_workflow` 图片补偿任务加 scheduler/限流适配 | Python 测试 + F5 日志核对 | 是 |
| P0 | 资源准备阶段按对象输出耗时与状态：pending/running/done/failed | Python 日志测试 + F5 | 是 |
| P0 | Demo 路径优先复用已有资产，减少实时文生图 + 图生 3D 数量 | Python/配置核对 + F5 | 是 |
| P1 | 阶段感知意图分类：未生成前的 edit 降级为 plan refinement | Agent adapter/Coordinator 测试 | 否，但强烈建议 |
| P1 | 保留否定偏好：`不要太恐怖` -> `avoid: 太恐怖` | 信息提炼测试 | 否，但影响效果 |
| P1 | 动态介入在资源准备阶段进入 pending resource/batch update | Coordinator/Worker 测试 + F5 | 否，但影响动态介入效果 |

## 2026-06-18 完整生成复盘：分批边界错误、后续介入未驱动新增生成

### 基本信息

- 日志文件：`E:\corona\CoronaEngine\build\examples\engine\RelWithDebInfo\logs\2026-06-18_15-52-01_corona.log`
- 证据来源：用户提供完整聊天室 transcript + 最新日志。
- 本次运行结果：场景最终生成完成，导入 8 个物体；但用户体验上，分批生成仍更像“一次性连续执行”，没有做到大分批、按用户介入调整后续生成计划。

### 关键证据

真实生成完成：

- `16:48:33`：地形创建完成：`地形(terrain)已创建: 18.5×18.5 m`
- `16:48:34`：边界铺设：`边界已铺设: kind=fence material=wood height=0.90`
- `16:48:34`：`ProgressiveWorkflow phase INTERIOR: 2 assets`
- `16:48:58`：批 `r1_INTERIOR` 完成，导入 `长桌、地毯`
- `16:48:58`：`ProgressiveWorkflow phase OBJECTS#1: 3 assets`
- `16:49:34`：批 `r1_OBJECTS_b1` 完成，导入 `摊位、入口拱门、导视牌`
- `16:50:03`：批 `r1_OBJECTS_b2` 完成，导入 `展示架、绿植`
- `16:50:17`：批 `r1_DECORATION` 完成，导入 `灯笼`
- `16:50:17`：`FinalReview: 保留 0 / 调整 2 / 待确认 0`

但资源准备早于这些批次完成，且是整体准备：

- `16:24:33`：调用原 `model_retrieval workflow（8 个物体，文生图→图生3D）`
- `16:24:33`：8 个元素无生成图片，统一进入补偿重试
- 后续资源准备与模型生成持续到导入阶段之前，用户介入期间没有改变资源列表。

地形边界问题：

- `16:54:08` 到 `16:54:17`：日志显示系统能获取并操作 `__terrain_boundary`，包括 `Applied SetVisible[0/1] to __terrain_boundary`
- `16:54:54`：工具报错 `Actor '_terrain_boundary' not found`
- 用户实际说的是 `_terrain_boundary`，而真实 actor 是 `__terrain_boundary`，工具/Agent 没做别名或相似匹配。

新增模型失败：

- 用户在生成中提出“再加一个天使雕像”“再增加一只小狗”。
- 最终完成报告只包含：`长桌、地毯、摊位、入口拱门、导视牌、展示架、绿植、灯笼`
- 后续多次请求添加天使雕像，系统均转为 edit 或要求提供模型路径：
  - `16:57:51`：`在场景再加入的天使雕像呢` 被分类为 `edit`
  - `16:59:00`：`“添加天使雕像` 被分类为 `edit`
  - `16:59:55`：`用户确认意图：添加生成一个天使雕像` 仍被分类为 `edit`

### 已确认问题

#### P0：分批边界放错，图片/模型资源没有按批同步生成

证据等级：[日志暴露的实际问题] / [用户体验确认]

现象：

- UI 后期显示 INTERIOR / OBJECTS#1 / OBJECTS#2 / DECORATION 等批次。
- 但日志显示在进入这些批次前，`model_retrieval workflow` 已按 8 个物体整体准备资源。
- 用户在 32% 后提出“天使雕像”“小狗”，但后续批次仍按最初 8 个物体执行，未改变资源计划。

判断：

当前“分批”主要是导入/摆放阶段的批次，不是终极目标要求的“计划 -> 图片生成 -> 模型生成 -> 导入”的大分批闭环。真正要支持动态介入，批次边界必须前移到资源计划层：每批只生成本批需要的图片和模型，批次之间读取最新用户介入，再决定下一批资源。

目标态：

- 第 1 批：确认本批对象 -> 生成图片 -> 生成模型 -> 导入 -> 暴露可介入窗口。
- 用户介入：新增/修改/删除进入 pending batch delta。
- 第 2 批：根据介入重算对象清单，再生成本批图片/模型/导入。

下一步改进：

- 将 `model_retrieval workflow` 从“全量 8 个物体一次性准备”改为按 batch plan 调用。
- `ProgressiveWorkflow` 每个 batch 开始前先读取 Coordinator pending interventions。
- 图片生成、模型生成、导入必须共享同一个 `batch_id`，而不是资源全局准备、导入再分批。

#### P0：用户生成中介入没有改变后续生成计划

证据等级：[用户 transcript 暴露的实际问题] / [日志结果确认]

现象：

- 用户在生成中提出：
  - `调整：再加一个天使雕像`
  - `新增：再增加一只小狗`
  - `说明：组装要给我好一点`
- 系统回复“会优先进入下一批或最终调整”。
- 但最终导入 8 个物体里没有天使雕像、小狗。
- 最终 `FinalReview` 只做了 2 个自动调整，没有新增模型。

判断：

当前 pending intervention 更像聊天侧记录，并没有进入资源计划和后续 batch object list。对用户来说，这就是“嘴上说下一批吸收，实际没有吸收”。

下一步改进：

- 明确 intervention 类型：
  - `add_object`：进入下一批资源生成计划。
  - `modify_style`：影响剩余对象 prompt / material / placement。
  - `assembly_quality`：进入 final review / AABB / spacing 规则。
- 当用户新增对象发生在资源准备期间，若目标批次尚未导入，应允许插入下一批或追加新批。
- 最终报告必须列出“已吸收介入 / 未吸收介入及原因”。

#### P0：生成后新增模型能力没有闭环

证据等级：[用户 transcript 暴露的实际问题] / [日志确认]

现象：

- 用户问“天使雕像呢”，系统只说当前 actor 列表没有。
- 用户说“生成添加呀”“添加生成一个天使雕像”。
- GM 确认后仍返回“未提供模型路径，现有可编辑 actor 中也没有天使雕像，因此暂未新增”。
- 日志显示这类请求多次被分类为 `edit`，进入已有 8 个 actor 的编辑通道。

判断：

系统缺少 post-generation add-object 流程。用户要求“生成添加一个新物体”时，应触发小型资源生成 + 导入批次，而不是要求用户提供模型路径。

下一步改进：

- 增加 `post_generation_add_object` intent。
- 确认后启动单对象或小批次流程：图片生成 -> 模型生成/检索 -> 导入 -> 放置 -> AABB/VLM 检查。
- 若 provider 不可用，应明确显示“当前真实模型生成 provider 未启用”，而不是泛称缺模型路径。

#### P0：地形/边界组件存在，但 AI 助手工具找不到

证据等级：[日志暴露的实际问题]

现象：

- 日志能读取 `__terrain_boundary`，并成功执行可见性切换。
- Agent 工具调用报错：`Actor '_terrain_boundary' not found`。
- 用户指出“我说的是 _terrain_boundary”，AI 仍无法定位真实地形边界组件。

判断：

这里不是地形组件不存在，而是 actor 命名、暴露、别名映射和工具查询范围不一致。真实 actor 名是 `__terrain_boundary`，用户/Agent 使用 `_terrain_boundary`，工具未做 fuzzy/alias 匹配，也没有把系统组件暴露为可编辑对象。

下一步改进：

- 建立 system actor alias 表：
  - `_terrain_boundary` -> `__terrain_boundary`
  - `terrain boundary / 栅栏 / 边界 / 地形边界` -> `__terrain_boundary`
  - `terrain / 地形 / 地面` -> `__room_terrain`
- 可编辑列表要区分普通 actor 与 system actor，但允许房主/GM 修改安全字段，如 visibility、material、height、style。
- 工具查找失败时返回相似候选，不要直接说找不到。

#### P1：修改引导不好用，Agent 错改对象

证据等级：[用户 transcript 暴露的实际问题] / [日志确认]

现象：

- 用户说“这个栅栏有点奇怪”。
- 商人建议换成低矮木栏/藤蔓围栏。
- 用户确认“换成更幻想集市风的低矮木栏/藤蔓围栏。”
- 商人把“入口拱门”调整成低矮围栏感，而用户实际指的是地形边界。

判断：

编辑通道缺少 target grounding。Agent 仅根据自然语言猜目标，未结合用户点击、最近选中对象、actor 候选、系统边界组件。

下一步改进：

- 修改前先做 target disambiguation：
  - 若目标词“栅栏/边界”可对应多个 actor，先列候选让用户确认。
  - 若 viewport pick 有对象，优先用选中对象。
  - 系统边界 actor 进入候选。
- 高风险修改不应直接落到“入口拱门”这种猜测目标。

#### P1：状态查询仍被当成介入或 GM 提案

证据等级：[用户 transcript 暴露的实际问题]

现象：

- 用户问“查看生成情况”“生成到哪里了”“到哪步了”。
- 系统多次先记录为介入，甚至在完成后为“查看生成情况”生成 GM 提案。

判断：

`status_query` intent 仍未稳定生效。查询进度应读当前 session/batch/job 状态，不应进入 pending intervention，也不应产生 GM 冲突提案。

下一步改进：

- 将“查看生成情况 / 到哪步了 / 生成到哪里了”固定路由为 status query。
- status query 只读状态，不修改 plan、不进入 intervention、不创建 proposal。

### 本次结论

本次运行证明真实生成链路已经能完成，但距离终极目标还有关键差距：

1. 当前分批是“导入分批”，不是“资源计划-图片-模型-导入”的完整批次闭环。
2. 用户介入被记录，但没有改变后续 batch 的资源生成计划。
3. 生成后新增模型没有闭环，仍被误走 edit / 需要模型路径。
4. 地形边界 actor 存在，但 AI 工具找不到，说明 system actor 暴露与别名映射缺失。
5. 修改引导缺少目标确认，容易错改对象。

这说明下一阶段应把“批次边界前移到资源计划层”和“post-generation add-object”列为 P0，而不是只优化 UI 文案。

### 后续任务建议

| 优先级 | 任务 | 验证方式 | 是否阻断终极计划验收 |
| --- | --- | --- | --- |
| P0 | 将图片生成、模型生成、导入改为按 batch 同步执行 | Python 协议测试 + F5 日志核对 | 是 |
| P0 | batch 开始前读取 pending interventions 并重算本批资源计划 | Coordinator/ProgressiveWorkflow 测试 | 是 |
| P0 | 增加 post-generation add-object 流程，支持生成并导入新模型 | Python 测试 + F5 | 是 |
| P0 | system actor alias：`_terrain_boundary` / 栅栏 / 边界 -> `__terrain_boundary` | 工具层测试 + F5 | 是 |
| P1 | 修改前 target disambiguation，避免错改入口拱门 | Agent adapter/工具测试 | 否，但强烈建议 |
| P1 | status_query 路由固定，不进入介入/GM 提案 | Coordinator/Agent adapter 测试 | 否，但强烈建议 |
| P1 | 最终报告列出已吸收和未吸收介入 | Worker/前端测试 | 否，但影响可信度 |

---

## 2026-06-18 本轮代码修复记录：批次闭环与长周期记忆骨架

### 已落地

- 新增 `SceneDesignContract`：SeedPlan 确认时创建房间级长周期场景契约，记录 scene type、mood、style、avoid、terrain/boundary、scale/placement rules，并随介入更新版本。
- 修复草案期多轮讨论只保留第一句的问题：未冻结 SeedPlan 下的后续聊天会真正写入 participants 与 intent summary，避免方案确认时丢失“夜晚幻想集市/灯光/休息区/风格统一”等后续约束。
- 修复 status query 路由：`生成到哪里了 / 查看生成情况 / 为什么不执行` 走只读状态查询，不再创建 intervention、proposal 或生成任务。
- 增加 post-generation add 路由：主生成完成后 `添加生成一个天使雕像` 会进入 `post_generation_add`，不再伪装成“缺少模型路径”的普通编辑。
- 新增 `TerrainComponentResolver`：幻想夜市自动派生低矮装饰边界 `low_decorative_boundary / vine_wood_lantern`，避免复用草原蒙古包木栅栏语义。
- 增加 system actor alias：`_terrain_boundary / terrain_boundary / 栅栏 / 边界 / 地形边界` 统一解析为 `__terrain_boundary`。
- ProgressiveWorkflow 新增 `BatchResourcePlan` 与 pending resource request：当中途新增“天使雕像/小狗”且当前 resolved 列表没有该物体时，不再只记为 `deferred_missing_asset`，而是产生下一批资源生成请求。

### 已自动验证

- `python editor/plugins/AITool/services/test_interaction_coordinator.py`
- `python editor/plugins/AITool/cai_extensions/agent/test_scene_composer_progressive_geometry.py`
- `python editor/plugins/AITool/cai_extensions/agent/test_terrain_style_profile.py`
- `python editor/plugins/AITool/services/test_memory_scope.py`
- `python editor/plugins/AITool/services/test_lanchat_agent_orchestrator.py`
- `python -m py_compile` 覆盖本轮修改的 Coordinator、contract、terrain resolver、SceneComposer、ProgressiveWorkflow。

### 待 F5/实机验证

- pending resource request 是否真正接入后续图片生成 -> 模型生成/检索 -> 导入链路。
- post-generation add 是否能在真实 provider 可用时创建单物体追加批次并导入场景。
- `_terrain_boundary` alias 在真实 Agent 工具编辑链中是否能定位并修改系统边界 actor。
- 前端是否清晰展示批次资源状态：图片生成、模型生成、导入、可介入窗口、已吸收/未吸收介入。

---

## 2026-06-18 追加修复：pending resource request 接入批次模型检索

### 已落地

- `ProgressiveWorkflow` 在每个 phase/batch 开始时读取 `session.pending_resource_requests`。
- 对 `planned` 状态的资源请求按小批量执行 `composer._run_model_retrieval()`，避免一次性资源爆发。
- 模型检索成功时，把新增物体转成当前批次资产，进入后续 `incremental_import()`。
- 模型 provider 不可用或检索失败时，不再伪造模型路径；请求会以 `provider_unavailable` 或 `failed` 状态保留，便于最终报告和 F5 复盘解释。
- 新增 `BatchResourcePlan` 自动记录 `plan_id / batch_id / phase / contract_version / requested_items / absorbed_interventions`，让跨批次风格契约与用户介入能够被追踪。

### 已自动验证

- `python editor/plugins/AITool/cai_extensions/agent/test_scene_composer_progressive_geometry.py`
- `python -m py_compile editor/plugins/AITool/cai_extensions/agent/scene_composer_progressive.py editor/plugins/AITool/cai_extensions/agent/test_scene_composer_progressive_geometry.py`

### 待 F5/实机验证

- 用户在第 1/2 批中说“新增天使雕像/小狗”后，日志应出现 resource request -> model retrieval -> import 的同批或下一批链路。
- 真实模型 provider 慢或失败时，聊天室应明确说明“模型生成服务不可用/失败”，而不是要求用户提供模型路径。
- 需要继续观察资源调度压力：新增小批次是否降低一次性图片/模型生成峰值。

---

## 2026-06-18 追加修复：最终报告披露未完成介入

### 已落地

- `FinalReviewReport.to_user_text()` 增加“仍待后续处理”段落。
- Coordinator 汇总的 deferred interventions 会进入最终用户报告，不再只存在内部结构里。
- `SceneSession.final_review()` 会把失败/不可用的 `batch_resource_plan` 转成用户可读的 deferred item。
- 如果模型 provider 不可用，报告会显示“模型生成服务暂不可用”，并且继续隐藏 provider、prompt、token、job 等内部字段。

### 已自动验证

- `python editor/plugins/AITool/cai_extensions/agent/test_scene_session.py`
- `python -m py_compile editor/plugins/AITool/cai_extensions/agent/scene_session.py editor/plugins/AITool/cai_extensions/agent/test_scene_session.py`

### 待 F5/实机验证

- 最终场景报告应列出“已吸收/仍待处理/失败原因”，尤其是天使雕像、小狗这类中途新增对象。
- 失败原因应面向用户表达，不暴露内部 provider、prompt、job id、scheduler payload。

---

## 2026-06-18 追加修复：GM 确认后的 post-generation add 结构化执行

### 已定位

- F5 复盘中“添加生成一个天使雕像”虽然能被 Coordinator 识别为 `post_generation_add`，但 GM 确认后的 Host 执行器只把 `start_generation / execute_seed_plan` 交给结构化 Coordinator。
- 因此追加生成容易退回自然语言 agent 执行，表现为“当前没有模型路径，请提供素材路径”，而不是创建追加生成批次。

### 已落地

- `LanChatHostActionExecutor` 将 `post_generation_add` 纳入结构化动作白名单，避免二次自然语言重分类。
- `InteractionCoordinator.execute_action_payload()` 增加 `post_generation_add` 分支：
  - 创建 `InterventionRequest(apply_policy="post_generation_add")`；
  - 提交 `scene_generation_append` 任务；
  - payload 明确携带 `append_mode=True`、`max_items=2`、`pending_interventions`、`scene_design_contract`；
  - 返回“追加生成请求已进入生成队列”，不再要求用户提供模型路径。
- 增加执行中保护：如果主生成仍处于 `confirmed/executing/paused`，GM 确认的新增对象不会另开追加 job，也不会覆盖当前 active generation session，而是记录为 `next_batch` 介入，等待下一批吸收。
- `SceneComposerJobRunner` 对追加任务做小批次约束：
  - composer `max_items` 默认收窄到 2；
  - prompt 明确为“只追加本次新增对象，不重建整个场景，不覆盖已有布局”；
  - 把 `SceneDesignContract.asset_style_prompt` 拼入追加 prompt，降低跨批次风格漂移。
- 记忆记录中只保存 job_id/status/session_id/plan_id，避免把 runtime context 或内部 payload 泄漏进用户可见状态。

### 已自动验证

- `python editor/plugins/AITool/services/test_interaction_coordinator.py`
- `python editor/plugins/AITool/services/test_generation_scheduler.py`
- `python editor/plugins/AITool/services/test_lanchat_agent_orchestrator.py`
- `python -m py_compile editor/plugins/AITool/services/interaction_coordinator.py editor/plugins/AITool/services/lanchat_host_action_executor.py editor/plugins/AITool/services/generation_composer_adapter.py editor/plugins/AITool/services/test_interaction_coordinator.py editor/plugins/AITool/services/test_generation_scheduler.py`

### 待 F5/实机验证

- 房主确认 `@GM 添加生成一个天使雕像` 后，应进入追加小批次，而不是普通聊天/模型路径询问。
- 主生成过程中确认新增对象时，应保持当前批次 session 有效，并在下一批前吸收，不应让当前批次进度事件被判 stale。
- 追加批次应只生成新增对象，不应重建整个夜市。
- 追加对象应继承“温暖、神秘但不恐怖的夜晚幻想集市”风格契约。
- 如果真实模型生成 provider 不可用，应明确披露“模型生成服务不可用/失败”，并保留为待处理介入。

---

## 2026-06-18 追加修复：系统边界 target grounding，避免错改入口拱门

### 已定位

- F5 复盘中用户说“栅栏/边界”时，Agent 曾把目标猜成“入口拱门”，说明编辑链路只把普通 actor 暴露给 LLM，系统边界 actor 被排除后缺少可靠 grounding。
- “换成低矮木栏/藤蔓围栏”不在快速编辑关键词内，容易绕过本地低风险编辑路径，进入自然语言工具链后由 LLM 猜目标。

### 已落地

- `MasterAgent` 的快速编辑识别词新增 `换成 / 低矮 / 藤蔓 / 木栏 / 围栏 / 栅栏 / 边界`。
- `_terrain_boundary / __terrain_boundary / terrain_boundary / 地形边界 / 栅栏 / 边界` 通过 `TerrainComponentResolver.canonical_actor_id()` ground 到 `__terrain_boundary`。
- `__terrain_boundary` 作为系统边界 actor 被允许进入编辑候选列表；其他 `__room_ / __interior_ / __terrain_` 基础设施仍默认不暴露，避免误改地形/草簇等派生组件。
- 对系统边界的低风险修改：
  - “低矮/太高/矮一点”会优先降低边界 Y scale；
  - “藤蔓/木栏/木质/温暖/自然”会尝试调整为自然木藤色；
  - 如果当前工具没有材质能力，会明确说明已定位系统边界并避免误改其他物体。
- 编辑 prompt 中标注 `kind=system_boundary`，让后续工具链知道这是系统边界而不是普通入口物件。

### 已自动验证

- `python editor/plugins/AITool/cai_extensions/agent/test_role_registry.py`
- `python -m py_compile editor/plugins/AITool/cai_extensions/agent/agent_adapter.py editor/plugins/AITool/cai_extensions/agent/test_role_registry.py`
- `python editor/plugins/AITool/services/test_lanchat_agent_orchestrator.py`

补充：`test_agent.py` 直接运行缺少包路径；设置 `PYTHONPATH=.` 后可进入测试，但受当前环境缺少 `CoronaCore`、真实协同状态和外部模型 key 影响，停在既有环境失败项 `76/82 passed`，不作为本轮通过门槛。

### 待 F5/实机验证

- 用户说“这个栅栏有点奇怪 / 换成低矮木栏或藤蔓围栏”时，不应再修改“入口拱门”。
- 如果场景存在 `__terrain_boundary`，Agent 应优先定位系统边界 actor。
- 真实工具层是否支持材质/风格替换仍需实机观察；若不支持，应至少做低风险高度/颜色调整，并给出可理解说明。

---

## 2026-06-18 追加修复：批次资源状态披露

### 已定位

- 复盘要求前端展示“当前批次、图片/模型/导入进度、可介入窗口、已吸收/未处理调整”。
- 当前 `SceneSession` 已有结构化进度，但用户可见文案主要是“准备放入/已放入”，对 pending resource request 的模型生成状态不够可见。
- 当前代码事实：后续新增对象已接入模型检索/生成和导入闭环；图片生成尚未形成独立批次节点。因此本轮不在文案里伪造“图片生成已完成”，只披露已实现的资源准备/模型/导入状态。

### 已落地

- `SceneSession.progressive_compose()` 在每个 batch 完成事件中携带本批 `batch_resource_plan`。
- `SceneSession.format_progress_message()` 新增用户可读资源状态：
  - `资源准备：新增请求 N 个`
  - `对象：天使雕像/小狗...`
  - `模型 x/y`
  - `失败/待重试 n`
  - `导入 x/y`
- 如果新增资源 provider 不可用、模型失败、或没有生成可导入资产，进度消息仍会披露失败/待重试，不再静默跳过。
- 继续过滤 provider、prompt、job、session、runtime context 等内部字段，避免把工具链细节暴露给用户。

### 已自动验证

- `python editor/plugins/AITool/cai_extensions/agent/test_scene_session.py`
- `python editor/plugins/AITool/cai_extensions/agent/test_scene_composer_progressive_geometry.py`
- `python editor/plugins/AITool/services/test_lanchat_agent_orchestrator.py`
- `python -m py_compile editor/plugins/AITool/cai_extensions/agent/scene_session.py editor/plugins/AITool/cai_extensions/agent/test_scene_session.py editor/plugins/AITool/cai_extensions/agent/test_scene_composer_progressive_geometry.py`

### 待 F5/实机验证

- 中途新增“天使雕像/小狗”后，聊天室进度应显示新增请求、模型完成/失败、导入数量。
- 如果 provider 不可用，应显示失败/待重试，而不是卡在 0% 或要求用户提供模型路径。
- 图片生成仍需后续单独接成批次节点；当前披露不会假称已经做了图片批次生成。

---

## 2026-06-18 追加修复：pending 新增对象接入显式图片阶段

### 已定位

- `SceneComposer._run_model_retrieval()` 旧 workflow 内部本来支持“文生图 -> 图生 3D/检索”，但 ProgressiveWorkflow 看不到这个阶段，导致批次状态上仍像是直接模型生成。
- 终极计划要求每批形成“图片生成 -> 模型生成/检索 -> 导入”的闭环，因此需要把图片阶段显式纳入 `BatchResourcePlan`，而不是只依赖 workflow 内部补偿逻辑。

### 已落地

- `BatchResourcePlan` 增加 `image_status / model_status / import_status`。
- pending resource request 处理流程改为：
  1. `image_generating`：优先调用 `composer._run_batch_image_generation()`；若没有专用 hook，则尝试现有 `generate_image` 工具；
  2. 图片成功时记录 `image_generated`，并把 `image_url` 注入后续模型检索 item；
  3. 图片 provider 不可用或图片失败时，记录 `image_provider_unavailable / failed`，但不中断模型阶段，继续降级到文字直生 3D；
  4. `model_generating`：执行 `_run_model_retrieval()`；
  5. 模型成功进入当前批次 assets，后续仍走 incremental import。
- `SceneComposer._run_model_retrieval()` 支持读取 item 上的 `image_url / generated_image_url`，写入旧 workflow 的 `global_assets.multi_scene.generated_images`，避免旧 dispatch 再重复文生图。
- 用户进度文案现在会显示 `图片 x/y`、`模型 x/y`、`导入 x/y`，同时不暴露 provider、prompt、job、session 等内部字段。

### 已自动验证

- `python editor/plugins/AITool/cai_extensions/agent/test_scene_composer_progressive_geometry.py`
- `python editor/plugins/AITool/cai_extensions/agent/test_scene_session.py`
- `python editor/plugins/AITool/services/test_lanchat_agent_orchestrator.py`
- `python -m py_compile editor/plugins/AITool/cai_extensions/agent/scene_composer_progressive.py editor/plugins/AITool/cai_extensions/agent/scene_composer.py editor/plugins/AITool/cai_extensions/agent/scene_session.py editor/plugins/AITool/cai_extensions/agent/test_scene_composer_progressive_geometry.py editor/plugins/AITool/cai_extensions/agent/test_scene_session.py`

### 待 F5/实机验证

- 新增“天使雕像/小狗”后，日志应出现 image stage -> model retrieval/generation -> import 的同批或下一批链路。
- 图片 provider 缺失时，应显示图片 `0/1` 或失败/待重试，并继续尝试文字直生 3D，不应卡死。
- 如果图片成功，旧 model_retrieval workflow 应使用已生成图片，不应重复触发同一对象的文生图。

---

## 2026-06-18 追加修复：pending 新增资源队列节流与积压披露

### 已定位

- F5 体验中“资源溢出/速度慢”不仅来自 provider 本身，还来自新增介入连续进入队列后，用户看不见哪些进了本批、哪些排到下一批。
- 代码已有 `CORONA_PROGRESSIVE_RESOURCE_REQUESTS_PER_BATCH` 每批处理数量限制，但没有把剩余 planned 请求作为 backlog 披露；`pending_resource_requests` 也缺少上限保护。

### 已落地

- 每批只处理 `CORONA_PROGRESSIVE_RESOURCE_REQUESTS_PER_BATCH` 个新增资源请求，默认 2。
- 剩余 planned 请求会生成 `resource_backlog` 任务，并进入用户可见进度：
  - `下一批资源队列还有 N 个`
  - `包括：小狗、喷泉...`
- 新增 `CORONA_PROGRESSIVE_PENDING_RESOURCE_LIMIT`，默认 16，限制房间内 pending 新增资源队列长度。
- 超出上限时保留最新请求，较早请求被裁剪并记录 `overflow_trimmed`，避免无界积压导致内存/状态膨胀。
- 进度文案会披露“已延后/下一批资源队列”，但仍不暴露 provider、prompt、job、session 等内部字段。

### 已自动验证

- `python editor/plugins/AITool/cai_extensions/agent/test_scene_composer_progressive_geometry.py`
- `python editor/plugins/AITool/cai_extensions/agent/test_scene_session.py`
- `python editor/plugins/AITool/services/test_lanchat_agent_orchestrator.py`
- `python -m py_compile editor/plugins/AITool/cai_extensions/agent/scene_composer_progressive.py editor/plugins/AITool/cai_extensions/agent/scene_session.py editor/plugins/AITool/cai_extensions/agent/test_scene_composer_progressive_geometry.py editor/plugins/AITool/cai_extensions/agent/test_scene_session.py`

### 待 F5/实机验证

- 连续提出多个新增对象时，当前批次只处理小批量，剩余应显示为“下一批资源队列”。
- 多次连续介入不应造成资源请求无限积压；超过上限时应有明确延后记录。
- 需要观察真实 provider 慢响应下，队列披露是否足以缓解用户焦虑，并避免重复点击触发更多资源风暴。

---

## 2026-06-18 追加修复：最终复核接入长周期 SceneDesignContract

### 已定位

- `SceneDesignContract` 已经在 SeedPlan 确认后创建，并进入批次资源计划，但最终复核报告没有明确把“长周期风格/避让/地形/边界/比例/摆放约束”反馈出来。
- F5 实测暴露的核心风险是：分批 + 中途介入后，最后组装可能偏离“温暖、神秘但不恐怖、风格统一的夜晚幻想集市”，尤其新增物体和边界/地形派生容易把跨批风格打散。

### 已落地

- `SceneSession.progressive_compose()` 新增 `scene_design_contract_provider` 注入点。
- `run_progressive_workflow()` 在 FinalReview 前从 `InteractionCoordinator.scene_design_contract(plan_id)` 读取当前 contract。
- `FinalReviewReport` 增加 `style_contract_summary`，只保留用户可理解字段：
  - 风格 / 氛围 / 色调 / 灯光；
  - 负向约束，如 `too horror / dark horror`；
  - 地形、边界、比例、摆放规则。
- 最终报告现在会输出：
  - `风格收口：...`
  - `已持续避开：...`
  - `组装约束：...`
- 报告继续过滤 `prompt / provider / job_id / session_id / token / vlm_raw / debug` 等内部字段，避免把内部链路暴露给聊天室用户。

### 已自动验证

- `python editor/plugins/AITool/cai_extensions/agent/test_scene_session.py`
- `python editor/plugins/AITool/cai_extensions/agent/test_scene_composer_progressive_geometry.py`
- `python editor/plugins/AITool/services/test_interaction_coordinator.py`
- `python editor/plugins/AITool/services/test_generation_scheduler.py`
- `python editor/plugins/AITool/services/test_lanchat_agent_orchestrator.py`
- `python -m py_compile editor/plugins/AITool/cai_extensions/agent/scene_session.py editor/plugins/AITool/cai_extensions/agent/scene_composer_progressive.py editor/plugins/AITool/cai_extensions/agent/test_scene_session.py`

### 待 F5/实机验证

- 完整生成结束后，最终报告应能看到风格收口、避让项、组装约束，而不是只列“放了哪些模型”。
- 中途新增天使雕像/小狗后，最终风格仍应贴合当前 contract；如果 provider 不可用，应列入未处理原因。
- `_terrain_boundary` 调整后，最终报告应体现边界约束仍是当前场景派生的低矮装饰边界，而不是草原蒙古包木栅栏逻辑。

---

## 2026-06-18 追加修复：最终报告合并 VLM / 外观检查状态

### 已定位

- ProgressiveWorkflow 已经能运行 VLM 外审，并把 actionable advice 写回 Coordinator ReviewResult。
- 但代码顺序是先生成 `final_report_text`，再运行 VLM 外审；因此最终报告文本本身不一定列出“VLM 是否执行、是否跳过、是否超时、是否发现建议”。
- 终极计划要求最终报告列出 VLM / 摆放检查状态，否则 F5 用户只能看到“最终检查”，但不知道外观审查是否真的发生过。

### 已落地

- `run_progressive_workflow()` 在 VLM 外审完成后，将 `vlm_review_text` 合并进 `final_report_text`：
  - `VLM/外观检查：VLM 审查未发现明显语义问题。`
  - 或 `VLM/外观检查：VLM 外审未完成：截图失败/跳过 N 个，超时 M 个；本轮以 AABB 几何检查为准。`
- `agent_adapter` 避免重复展示同一段 VLM 文本：如果最终检查里已经包含该 VLM 文案，就不再单独重复追加 `VLM 外审`。
- `VlmReviewReport.to_user_text()` 增加敏感字段裁剪，过滤：
  - `prompt / raw_prompt / provider / debug / job_id / session_id / token / api_key / vlm_raw / screenshot_dir / output_dir`
- VLM 仍保持外回路策略：建议不强制执行、失败不阻塞主生成链路。

### 已自动验证

- `python editor/plugins/AITool/cai_extensions/agent/test_vlm_review_loop.py`
- `python editor/plugins/AITool/cai_extensions/agent/test_scene_composer_progressive_geometry.py`
- `python editor/plugins/AITool/cai_extensions/agent/test_scene_session.py`
- `python editor/plugins/AITool/services/test_lanchat_agent_orchestrator.py`
- `python -m py_compile editor/plugins/AITool/cai_extensions/agent/vlm_review_loop.py editor/plugins/AITool/cai_extensions/agent/scene_composer_progressive.py editor/plugins/AITool/cai_extensions/agent/agent_adapter.py editor/plugins/AITool/cai_extensions/agent/test_vlm_review_loop.py editor/plugins/AITool/cai_extensions/agent/test_scene_composer_progressive_geometry.py`

### 待 F5/实机验证

- 完整生成报告中应出现 `VLM/外观检查`，即使 VLM 被跳过或超时，也应说明原因。
- VLM provider 或截图不可用时，不应卡住主生成，也不应让用户误以为审查已完整通过。
- 如果 VLM 建议中含底层路径、provider、prompt 或 raw 输出，聊天室最终报告不应泄漏。

---

## 2026-06-18 追加修复：queued 状态不再误显示为“生成中 0%”

### 已定位

- F5 复盘中出现过：任务返回 `(queued)`，但聊天室顶部/系统消息显示 `生成中 0%`。
- 根因在 `InteractionCoordinator.execute_confirmed_plan()`：提交 scheduler 后，不管 scheduler 返回 `queued` 还是 active，都会立即记录 `stage="executing"`。
- 这会让用户误以为模型已经开始生成，实际只是等待资源调度，容易触发重复追问和重复提交。

### 已落地

- `DisclosurePolicy` 增加：
  - `queued -> 排队中`
  - `waiting_resource -> 等待资源`
- queued 阶段的用户文案改为：
  - 房主：`排队中：生成任务已进入队列，正在等待资源调度。你可以继续补充要求，我会在后续批次前吸收。`
  - 普通用户：`排队中：生成任务已排队，正在等待资源；你可以继续提出调整，我会在下一批前吸收。`
- `InteractionCoordinator.execute_confirmed_plan()` 现在按 `GenerationJobRef.status` 披露阶段：
  - `queued` -> `排队中`
  - `waiting_user / paused` -> `等待资源`
  - active status -> `生成中`
  - done/completed -> `完成`
- `status_query` 读取最新 job status：
  - 如果 plan 已执行但 job 仍 queued，会回答“当前方案已进入生成队列，正在等待资源调度”，不再回答“正在生成中”。

### 已自动验证

- `python editor/plugins/AITool/services/test_disclosure_policy.py`
- `python editor/plugins/AITool/services/test_interaction_coordinator.py`
- `python editor/plugins/AITool/services/test_generation_scheduler.py`
- `python editor/plugins/AITool/services/test_lanchat_agent_orchestrator.py`
- `python -m py_compile editor/plugins/AITool/services/disclosure_policy.py editor/plugins/AITool/services/interaction_coordinator.py editor/plugins/AITool/services/test_disclosure_policy.py editor/plugins/AITool/services/test_interaction_coordinator.py`

### 待 F5/实机验证

- SeedPlan 进入队列但 provider/worker 尚未接管时，界面应显示“排队中/等待资源”，不应显示“生成中 0%”。
- 用户问“为什么不执行/生成到哪里了”时，应返回等待资源调度状态，不应产生新 proposal，也不应追加生成任务。
- 进入真实批次后，才切到“生成中/批次生成中”。

---

## 2026-06-18 追加修复：生成前风格补充不再误走 edit 空场景

### 已定位

- F5 复盘中房主在确认开始前补充：“我希望它更温暖一点，有灯光和休息区，不要全是暗黑风。”
- 该句被意图分类误判为 `edit`，进入 `_handle_edit()`，因为场景还没有生成，最终返回“场景里没有可编辑的物体”。
- 这不是用户真的要编辑已有 actor，而是对 pending 方案的风格/布局偏好补充。

### 已落地

- `LanChatSceneRuntime.handle_planning_gate()` 在 pending planning confirmation 阶段扩大“方案补充”识别范围。
- 新增生成前方案补充关键词：
  - `我希望 / 希望 / 更 / 不要 / 别 / 不能`
  - `风格 / 统一 / 一致 / 温暖 / 灯光 / 灯笼 / 休息区 / 暗黑风`
  - `不要太恐怖 / 不太恐怖 / 适合 / 整体`
- 当已经有 pending 方案时，上述补充会进入 `confirmation.constraints`，确认开始后随 compose prompt 一起进入生成链路。
- 只影响“生成前 pending 方案”阶段；生成中 busy note 和已有场景编辑路径不改。

### 已自动验证

- `python editor/plugins/AITool/services/test_lanchat_agent_orchestrator.py`
- `python -m py_compile editor/plugins/AITool/services/lanchat_scene_runtime.py editor/plugins/AITool/services/test_lanchat_agent_orchestrator.py`

### 待 F5/实机验证

- 在生成前说“更温暖、有灯光和休息区、不要全是暗黑风”时，聊天室不应再返回“场景里没有可编辑的物体”。
- 后续 `确认开始` 的生成 prompt 应包含这些补充要求。
- 如果用户明确说“放大/删除/移动某个已生成物体”，仍应走 edit 或生成中介入路径。

---

## 2026-06-18 追加修复：@Agent 状态查询不再进入模型/GM 提案路径

### 已定位

- F5 复盘中用户问 `@商人 查看生成情况`、`@商人 到哪步了`、`@GM 为什么不执行` 时，系统容易先记录为可介入窗口，再继续调用角色 Agent 或生成 GM 提案。
- 根因是 `InteractionCoordinator` 已经具备 `status_query` 只读判断，但 `LANChatAgentWorker` 只把普通聊天室消息同步给 Coordinator；对 `@Agent` trigger，状态查询同步后仍会继续进入 busy/runtime、角色模型或 GM proposal 路径。
- 这会造成两个问题：一是用户查询进度时得到“我会在下一阶段吸收”这种误导回复；二是查询类消息可能污染 pending intervention / proposal / SeedPlan 队列。

### 已落地

- `LANChatAgentWorker._process_trigger()` 在 GM pace / clarification 之后、角色 Agent 调用之前，新增 Coordinator `status_query` 短路。
- `@Agent 查看生成情况 / 到哪步了 / 生成到哪里了 / 为什么不执行` 现在会：
  - 调用 `InteractionCoordinator.ingest_message()`；
  - 只接收 `event_type == "status_query"` 的只读结果；
  - 直接向聊天室返回当前方案状态；
  - 不调用角色模型；
  - 不生成 GM proposal；
  - 不进入 host executor。
- 这个修复只影响查询类 trigger；真实生成、修改、删除、追加仍走原有 Coordinator / runtime / host executor 路径。

### 已自动验证

- `python editor/plugins/AITool/services/test_lanchat_agent_orchestrator.py`
- `python -m py_compile editor/plugins/AITool/services/lanchat_agent_worker.py editor/plugins/AITool/services/test_lanchat_agent_orchestrator.py`

新增测试覆盖：

- 有 active SeedPlan 时，`@商人 查看生成情况，到哪步了` 返回 Coordinator 状态。
- 状态查询不会调用模型 Agent。
- 状态查询不会进入 host executor。
- 状态查询不会产生 GM proposal。

### 待 F5/实机验证

- 生成中询问 `@商人 查看生成情况 / 到哪步了` 时，应只返回当前队列、生成或批次状态。
- 询问 `@GM 为什么不执行` 时，应解释排队/资源调度/当前批次状态，不应创建新的 SeedPlan 或 GM 提案。
- 聊天室不应再把查询类消息显示成“可介入窗口：你的补充会优先进入下一批”。

---

## 2026-06-18 追加修复：GM 确认/拒绝后前端按钮立即失效

### 已定位

- F5 复盘中出现过：房主拒绝某个 `gm-...` 提案后，消息气泡下方仍保留“确认 / 拒绝”按钮；再次点击时，后端只能返回“已处理 rejected，不会重复入队”。
- 后端幂等保护是对的，但前端继续展示按钮会让用户误以为该提案仍可操作，降低演示可信度。
- 根因是前端只在点击后调用 `dismissDisclosureByProposal()` 清掉顶部 disclosure，没有维护“已处理 proposal id”，也没有让消息气泡下方的 `gm_proposal` 按钮读取处理状态。

### 已落地

- `lanchat.js` 增加 `processedProposalIds`，并提供：
  - `markProposalHandled(proposalId)`
  - `isProposalHandled(proposalId)`
- 点击 GM 确认/拒绝后，RoomPanel 会立即调用 `markProposalHandled()`，消息气泡下方按钮即时隐藏。
- 收到 GM 回执消息时，store 会从“已确认 / 已取消 / 已拒绝 / 已处理 / 不会重复 / 不会执行”等文本中提取 `gm-* / fa-* / cr-*`，同步标记为已处理，覆盖用户手动输入 `@GM 确认 ...` 的路径。
- 顶部 disclosure 的 pending 优先级也改为只保留“未处理”的确认项；已处理提案不会继续压住常规进度状态。

### 已自动验证

- `node editor/Frontend/scripts/test-lanchat-roster.mjs`
- `node editor/Frontend/src/stores/lanchatDisclosure.test.mjs`
- `python editor/plugins/AITool/services/test_lanchat_agent_orchestrator.py`
- `python -m py_compile editor/plugins/AITool/services/lanchat_agent_worker.py editor/plugins/AITool/services/test_lanchat_agent_orchestrator.py`

### 待 F5/实机验证

- 点击某个 GM 提案的“确认”或“拒绝”后，该消息下方按钮应立即消失。
- 如果用户手动输入 `@GM 拒绝 gm-...`，收到 GM 回执后，该提案按钮也应消失。
- 后端幂等提示仍保留，但正常交互中用户不应再能反复点同一个已处理提案。

---

## 2026-06-18 追加修复：GenerationScheduler 生命周期日志可对账

### 已定位

- F5 复盘中多次出现 UI 显示 `queued / 生成中 0%`，但 `corona.log` 里难以按 `seed_id / generation_id / batch_id` 追踪任务到底卡在 submit、queue、worker、provider 还是 import。
- `GenerationScheduler` 已经维护 `recent_events`，但主要在内存 snapshot 里，不保证每个状态变化都落到日志。
- 缺少落盘日志会让后续 F5 一旦卡住，很难判断是 provider 慢、scheduler 暂停、队列拥堵、composer factory 缺失，还是 UI 映射错误。

### 已落地

- `GenerationScheduler._record_event_locked()` 现在会对每个安全事件写入 `logger.info`：
  - `event`
  - `room_id`
  - `session_id`
  - `plan_id`
  - `job_id`
  - `batch_id`
  - `status`
  - `stage`
  - `priority`
  - `cancelled_count / pruned_count`
- 日志只使用调度器已有白名单事件字段，不写入：
  - `prompt`
  - `provider`
  - `runtime_context`
  - `token / api_key`
  - 任何模型请求正文
- 覆盖的关键事件包括：
  - `submit`
  - `queue_full`
  - `status_change`
  - `cancel_requested`
  - `cancel_session`
  - `terminal_jobs_pruned`

### 已自动验证

- `python editor/plugins/AITool/services/test_generation_scheduler.py`
- `python -m py_compile editor/plugins/AITool/services/generation_scheduler.py editor/plugins/AITool/services/test_generation_scheduler.py`

新增测试覆盖：

- 调度器日志包含 room/plan/job/batch/status。
- 调度器日志不泄漏 prompt/provider/runtime token。

### 待 F5/实机验证

- 下一次 F5 运行时，`corona.log` 应能搜索到 `[GenerationScheduler] event=... plan_id=... job_id=... batch_id=...`。
- 如果生成卡住，应能根据最后一个 `status_change` 判断卡在 `queued / preparing / submitting / polling / downloading / importing / failed`。
- 日志中不应出现完整 prompt、provider secret、token 或 runtime context。

---

## 2026-06-18 追加修复：host-only disclosure fallback 可被房主端还原

### 已定位

- F5 复盘里多次出现系统消息只显示“有一项仅房主可见的进度更新。”或“有一项需要房主确认的事项。”。
- 这是因为 native/host targeted API 不可用时，`LANChatAgentWorker` 为避免泄漏房主专属信息，会把 host disclosure 降级成 participant-safe metadata。
- 降级策略本身是对的，但前端房主端也只能看到 participant-safe 版本，导致房主无法在顶部状态区看到真实确认事项、仲裁建议或进度内容。

### 已落地

- `LANChatAgentWorker._emit_new_disclosure_events()` 在 targeted host API 不可用时，现在会在同一条广播 metadata 中同时写入：
  - `disclosure`：参与者安全视图，只保留泛化文案和安全 proposal id。
  - `host_disclosure`：房主可恢复视图，保留房主应看到的确认文案、可操作按钮、proposal id 和安全元数据。
- `lanchatDisclosure.extractDisclosureFromMessage()` 增加 `role` 参数：
  - `role === "host"` 时优先解析 `metadata.host_disclosure`。
  - 非房主继续解析 `metadata.disclosure`，不会看到房主专属文案。
- `lanchat.js` 在从消息生成 disclosure card 时传入 `state.role`，避免房主端退化为普通参与者视图。
- 静态约束脚本增加检查，防止后续改动遗漏 role-aware fallback。

### 已自动验证

- `node editor/Frontend/src/stores/lanchatDisclosure.test.mjs`
- `node editor/Frontend/scripts/test-lanchat-roster.mjs`
- `python editor/plugins/AITool/services/test_lanchat_agent_orchestrator.py`
- `python -m py_compile editor/plugins/AITool/services/lanchat_agent_worker.py editor/plugins/AITool/services/test_lanchat_agent_orchestrator.py`

新增测试覆盖：

- 同一条 host-only fallback 广播中，普通成员只能解析到安全占位文案。
- 房主能从 `host_disclosure` 还原真实确认文案、可操作动作和 proposal id。
- disclosure metadata 仍过滤 `prompt/provider/raw_prompt/hidden_debug_ref` 等内部字段。
- 后端广播文本仍是安全占位，不在公开聊天气泡中泄漏房主专属内容。

### 待 F5/实机验证

- 当 targeted host API 不可用时，房主顶部状态区应显示真实确认/仲裁/进度内容，而普通参与者仍只看到安全占位。
- 房主端确认按钮应能基于 `host_disclosure.available_actions` 正常显示并执行。
- 聊天公开气泡中不应出现 prompt、provider、worker、queue、batch id 等内部链路信息。

---

## 2026-06-18 追加修复：GenerationScheduler 假完成与 Coordinator 状态不同步

### 已定位

- 本轮 F5 中，日志显示 `GenerationScheduler` 已将任务推进到 `done`：
  - `plan_id=seed-e398f10b1e97`
  - `job_id=gen-f968823102d9`
  - `queued -> preparing -> submitting -> polling -> downloading -> postprocessing -> importing -> done`
- 但聊天室查询 `@GM 现在生成到哪里了` 仍返回“当前方案正在生成中”，系统阶段条也显示“生成中 0%”。
- 同一日志中没有 `SceneComposer / ProgressiveWorkflow / IncrementalImport / AABB repair / FinalReview` 记录，说明调度器很可能跑了空 stage，而不是真正进入场景生成。
- 根因有两层：
  - `AITool.main` 创建 `LANChatAgentWorker` 时没有传 `composer_factory`，实机 worker 创建的 scheduler 没有 `SceneComposerJobRunner`。
  - `GenerationScheduler` 对没有 handler 的 stage 默认返回 `completed_stages`，导致 `scene_generation` 任务可以空跑到 `done`。
  - scheduler 终态没有回写 `InteractionCoordinator`，SeedPlan 仍停留在 `executing`，所以状态查询继续返回“生成中”。

### 已落地

- `AITool.main` 新增 `_create_lanchat_scene_composer()`，并向 `LANChatAgentWorker` 传入 `composer_factory`，实机调度器会使用 `SceneComposerJobRunner(stage_order=("compose",))`。
- `GenerationScheduler._run_stage_handler()` 对 `job_type == "scene_generation"` 缺少 stage handler 的情况直接失败，不再允许空跑假完成。
- `GenerationScheduler` 在 job 进入终态时调用 runtime context 中的 Coordinator 回调。
- `InteractionCoordinator.ingest_generation_job_status()` 新增终态吸收：
  - 更新保存的 generation job ref。
  - `done/completed` 时把 SeedPlan 标记为 `completed`。
  - 发出 completed disclosure。
  - 后续状态查询返回“当前主生成已完成”，不再显示“生成中 0%”。

### 已自动验证

- `python editor/plugins/AITool/services/test_generation_scheduler.py`
- `python editor/plugins/AITool/services/test_interaction_coordinator.py`
- `python editor/plugins/AITool/services/test_native_lanchat_bridge_static.py`
- `python editor/plugins/AITool/services/test_lanchat_agent_orchestrator.py`
- `python -m py_compile editor/plugins/AITool/main.py editor/plugins/AITool/services/generation_scheduler.py editor/plugins/AITool/services/interaction_coordinator.py editor/plugins/AITool/services/seed_plan.py editor/plugins/AITool/services/test_generation_scheduler.py editor/plugins/AITool/services/test_interaction_coordinator.py editor/plugins/AITool/services/test_lanchat_agent_orchestrator.py editor/plugins/AITool/services/test_native_lanchat_bridge_static.py`
- `codegraph.cmd sync E:\corona\CoronaEngine`

新增测试覆盖：

- `scene_generation` 缺少 stage handler 时必须 `failed`，不能 `done`。
- scheduler `done` 回调会把 SeedPlan 标记为 `completed`。
- `@GM 现在生成到哪里了` 在 job done 后不再返回“正在生成”。
- 插件主入口必须给 LANChat worker 传入 `composer_factory`。

### 待 F5/实机验证

- 下一轮 F5 触发生成后，日志中应出现 `SceneComposer / ProgressiveWorkflow / IncrementalImport`，而不是只出现 scheduler 秒级 `done`。
- 若 composer 配置缺失，任务应进入 `failed` 并暴露可诊断状态，不应显示“生成中 0%”或假完成。
- 生成完成后再次询问 `@GM 现在生成到哪里了`，应返回“当前主生成已完成”，并允许追加生成或最终调整。

---

## 2026-06-18 F5 复盘：真实生成已进入，但计划传递、地形派生、VLM 与速度仍有问题

### 本轮现象

- 聊天目标：房主先让长者和山贼讨论“温暖、神秘但不恐怖的室外夜集”，再要求长者简化方案并按方案执行。
- 生成确实进入了 `SceneComposer / model_retrieval / ProgressiveWorkflow / FinalReview`，不再是纯空跑。
- 但最终效果仍不理想：方案内容丢失、地形边界模板化、VLM 没有发挥作用，且整体耗时明显偏长。

### 日志证据

- SeedPlan `seed-9af1fa321e8f` / job `gen-a9055e702402` 在 `19:02:00` 被 `GenerationScheduler` 秒级推进到 `done`。
- 真正的 `SceneComposer` 是 `19:02:41` 才开始，说明当前仍存在“GM/SeedPlan 调度路径”和“RoleAgent 确认开始直接生成路径”没有完全收口的问题。
- F5 快照 `C:\Users\fzm\AppData\Local\Temp\corona_m2_f5_decompose\lanchat_scene_1781780601.json` 中的 prompt 仍是早期初始需求和 RoleAgent 软偏好，不是长者最终简化方案全文。
- `SceneComposer` 只提取到 6 个物体：`集市摊位 / 展示桌 / 灯笼 / 导视牌 / 入口拱门 / 长椅`。讨论中提到的暖光串灯、油灯、休息区细节、小火盆、热饮食物摊、蓝紫点缀、中央通道、安全边角等没有稳定进入结构化生成计划。
- 用户中途介入“后面再加入一个天使雕像”被吸收，但对象名变成了 `入一个天使雕像`，说明介入解析仍会残留动词/后缀，后续会污染 actor 名和模型检索。
- 地形创建日志：`19.2×19.2 m, type=flat, material=stone, scatter=paving_marks`；边界创建日志：`kind=wall material=stone height=0.80 ring_r=8.00`。这说明幻想夜市没有派生出低矮藤蔓/灯笼/局部边界，而是走了偏通用的平地+环形墙边界。
- VLM 日志为 `visual_review 审查阶段已关闭，全部放行`，最终 `FinalReview: 保留 0 / 调整 0 / 待确认 0`。本轮没有看到 VLM 对入口、主体、风格或整体布局产生实际建议。
- 速度瓶颈：
  - 6 个初始物体图片补偿 `elapsed=351.95s`。
  - 对象检索/入库多次出现 embedding 401，导致检索失败后降级生成。
  - 追加的天使雕像单物体又走了一轮检索失败 -> 生成流程。
  - 从 `SceneComposer` 开始到 `FinalReview` 约 27 分钟。

### 问题判断

1. 计划传递仍未闭环：最终讨论方案没有成为唯一 `SceneDesignContract / SeedPlan` 输入，RoleAgent “确认开始”路径仍可能绕开 GM 已确认方案。
2. 介入吸收可用但解析粗糙：中途新增能进入后续生成，但对象名和语义字段没有清洗干净。
3. 地形/边界派生没有真正按场景语义落地：`TerrainComponentResolver` 的目标方向是对的，但实机创建仍偏通用模板。
4. VLM 当前不是质量闭环的一部分：已存在外回路代码，但本轮配置关闭，最终检查只剩 AABB/规则类检查。
5. 速度问题主要来自资源侧：图片补偿慢、embedding 配置失败导致缓存检索失效、追加物体触发完整生成链路。

### 下一轮修复优先级

1. 收口执行入口：`确认开始/直接生成` 必须优先读取当前房间已确认 SeedPlan 和 SceneDesignContract，不允许 RoleAgent 使用旧 prompt 直接生成。
2. 修复 SeedPlan prompt：GM proposal 应只吸收最新有效方案和明确生成意图，过滤“现在生成到哪里了/我们开始了吗”等状态查询。
3. 修复介入对象抽取：`后面再加入一个天使雕像` 应抽取为 `天使雕像`，不能生成 `入一个天使雕像`。
4. 把 terrain/boundary spec 接入实机创建：幻想夜市应派生 `low_decorative_boundary / vine_wood_lantern / partial coverage`，不再默认通用环形墙。
5. 打开并限流 VLM 外审：先按终极计划执行“第一批关键入口/主体、中间批高风险新增、最终全局一致性建议”，只产建议，不直接改场景。
6. 速度优化：修复 embedding API 配置；本地命中优先；已存在资产跳过图片补偿；追加单物体走轻量追加批，不触发全量资源链路。

### 待 F5/实机验证

- 修复后再次执行同一脚本，F5 快照中的 prompt 应包含长者最终简化方案，而不是早期初始需求。
- 日志中 SeedPlan job 不应在真正 SceneComposer 启动前秒级 `done`。
- 天使雕像 actor 名应为 `天使雕像`。
- 地形/边界日志应显示夜市语义派生，而不是通用 `wall/ring`。
- VLM 日志应出现受控审查或明确降级原因。
- 总耗时应明显下降，至少不再因 embedding 401 导致可复用资产全部降级生成。

---

## 2026-06-18 追加修复：方案执行入口收口、介入对象清洗、夜市地形派生

### 已落地

- `InteractionCoordinator.execute_confirmed_plan()` 现在会向 scheduler payload 写入完整 `prompt / intent_text`，内容包含：
  - 已确认多人方案 `intent_summary`
  - 风格/空间/摆放约束
  - `SceneDesignContract.asset_style_prompt`
  - `terrain_spec / boundary_spec`
- `confirm_seed_plan()` 返回给 host executor 的 `intent_text` 也改为完整执行 prompt，并保留 `plan_summary` 作为摘要字段。
- `LANChatAgentWorker` 新增 Coordinator 优先的生成启动拦截：
  - 当房间已有 `CONFIRMED` SeedPlan 时，`@长者 确认开始 / 直接生成 / 按方案生成` 不再进入 RoleAgent planning gate。
  - 直接调用 `InteractionCoordinator.execute_confirmed_plan()` 入队。
  - 当 SeedPlan 已在 `EXECUTING` 时，返回当前状态，不重复启动。
- `InteractionCoordinator` 和 `LanChatSceneRuntime` 修复新增对象抽取：
  - `@长者 后面再加入一个天使雕像` -> `target_hint=天使雕像`
  - 不再产生 `入一个天使雕像`、`@长者` 这类污染对象名。
- `TerrainComponentResolver` 放宽幻想夜市识别条件：
  - 不再要求文本同时出现“幻想 + 集市”。
  - “神秘/温暖/灯光/夜晚 + 集市”也会派生 `fantasy_night_market`。
- `SceneComposer.apply_scene_semantic_terrain_profile()` 对幻想夜市增加泛化边界覆盖：
  - 若 LLM 给出的是通用 `wall/stone` 边界，会覆盖成 `fence/wood/vine_wood_lantern/partial/height=0.55`。

### 已自动验证

- `python editor/plugins/AITool/services/test_interaction_coordinator.py`
- `python editor/plugins/AITool/services/test_lanchat_agent_orchestrator.py`
- `python editor/plugins/AITool/cai_extensions/agent/test_scene_composer_progressive_geometry.py`
- `python editor/plugins/AITool/services/test_generation_scheduler.py`
- `python -m py_compile editor/plugins/AITool/services/interaction_coordinator.py editor/plugins/AITool/services/lanchat_agent_worker.py editor/plugins/AITool/services/lanchat_scene_runtime.py editor/plugins/AITool/services/terrain_component_resolver.py editor/plugins/AITool/cai_extensions/agent/scene_composer.py editor/plugins/AITool/services/test_interaction_coordinator.py editor/plugins/AITool/services/test_lanchat_agent_orchestrator.py editor/plugins/AITool/cai_extensions/agent/test_scene_composer_progressive_geometry.py`
- `codegraph.cmd sync E:\corona\CoronaEngine`

新增测试覆盖：

- 已确认 SeedPlan 存在时，`确认开始` 不再跑 RoleAgent，而是走 Coordinator 入队。
- scheduler payload 的 `prompt` 包含最终方案、负向约束、长周期合同和地形/边界约束。
- 后续新增对象会清洗动作词、数量词和 @mention。
- 温暖神秘室外集市会派生幻想夜市 profile。
- 泛化石墙边界会被夜市低矮装饰边界覆盖。

### 待 F5/实机验证

- 同一聊天脚本下，F5 快照 prompt 应包含“最终简化方案 / 灯火温暖 / 中央宽通道 / 两侧摊位 / 休息区”等内容。
- `@长者 确认开始` 不应再触发旧的 RoleAgent 直接 compose prompt。
- 日志中的地形边界应显示 `kind=fence material=wood height=0.55 style=vine_wood_lantern` 或等价语义，不再是 `kind=wall material=stone`。
- 中途新增天使雕像 actor 名应为 `天使雕像`。
- VLM 仍未在本轮默认开启；后续需要按终极计划单独接“受控外审 + 明确降级原因”，避免拖慢主链路。

---

## 2026-06-18 追加修复：VLM 外审状态可观测化

### 已落地

- `VlmReviewReport` 增加轻量状态字段：
  - `status=completed`：完成审查。
  - `status=disabled`：配置关闭，常见于 F5 演示模式。
  - `status=skipped`：没有可审查目标。
  - `status=unavailable`：依赖或调用异常。
- `run_progressive_workflow()` 中的 VLM 外回路不再在 `max_targets=0` 时静默返回 `None`。
- 当 `CORONA_F5_DEMO_MODE=1` 且未显式设置 `PROGRESSIVE_VLM_MAX_TARGETS` 时，最终报告会明确出现：
  - `VLM 外审未执行：F5 演示模式默认关闭 VLM，以避免截图/审查拖慢主链路；本轮以 AABB 几何检查和最终调整建议为准。`
- 显式设置 `PROGRESSIVE_VLM_MAX_TARGETS>0` 时，原有受控 VLM 外审仍可执行。
- `disabled/skipped` 状态不会误写入 Coordinator 的失败 review，避免把“配置关闭”当成场景问题。

### 已自动验证

- `python editor/plugins/AITool/cai_extensions/agent/test_scene_composer_progressive_geometry.py`
- `python editor/plugins/AITool/cai_extensions/agent/test_vlm_review_loop.py`
- `python -m py_compile editor/plugins/AITool/cai_extensions/agent/scene_composer_progressive.py editor/plugins/AITool/cai_extensions/agent/vlm_review_loop.py editor/plugins/AITool/cai_extensions/agent/test_scene_composer_progressive_geometry.py`
- `codegraph.cmd sync E:\corona\CoronaEngine`

新增测试覆盖：

- F5 demo mode 下 VLM 默认 target 数为 0，但最终报告必须显示未执行原因。
- 显式 `PROGRESSIVE_VLM_MAX_TARGETS=1/2` 仍可触发 composer hook + EngineGate + review hook。
- VLM actionable advice 仍能流入 Coordinator ReviewResult。

### 待 F5/实机验证

- 下一轮 F5 如果没有显式打开 VLM，最终报告和日志应清楚写出 VLM 未执行原因，而不是“看不出有没有调用”。
- 若要验证真实 VLM 外审，可在实机前设置 `PROGRESSIVE_VLM_MAX_TARGETS=1`，先只审 1 个高风险目标，观察截图、VLM 调用、超时兜底和最终建议是否正常。
---

## 2026-06-18 追加修复：资源链路提速，本地资产前置命中

### 已落地

- `model_retrieval_workflow.dispatch_node()` 在进入图片补偿前，先查询本地模型库和本地图片库：
  - 命中本地模型：直接生成检索任务，标记 `local_model_cached=True`，跳过图片补偿和后续图搜/生成等待。
  - 命中本地图片：直接使用缓存图片进入检索，不再调用图片补偿。
  - 只有模型/图片都未命中的对象，才进入图片补偿；补偿失败仍降级为 `__text_to_3d__` 文生 3D。
- `retrieve_single_item()` 支持读取 dispatch 传下来的 `model_path`，避免重复依赖图像检索；若本地模型标记失效，会明确转为 `local_library_miss` 的待生成任务。
- 新增离线测试覆盖：
  - 本地模型命中时不会进入图片补偿。
  - 本地图片命中时不会进入图片补偿。
  - 原有图片服务配置致命错误仍只探针一次，然后降级为 text-to-3D，不会 fan out 拖慢所有物体。

### 修复目标

- 对应 F5 里 `图片补偿 elapsed=351.95s` 的速度瓶颈。
- 已存在或曾经生成过的通用资产（灯笼、导视牌、长椅、桌子等）应尽量复用本地库，不再因为缺少本轮图片而等待长时间文生图补偿。
- 追加单物体批次如果命中本地库，应变成轻量追加；如果未命中，再走单物体图片/模型生成链路。

### 已自动验证

- `python editor/plugins/AITool/cai_extensions/flows/model_retrieval_workflow/test_dispatch_image_retry.py`
- `python editor/plugins/AITool/cai_extensions/agent/test_scene_composer_progressive_geometry.py`
- `python editor/plugins/AITool/services/test_lanchat_agent_orchestrator.py`
- `python editor/plugins/AITool/services/test_generation_scheduler.py`
- `python editor/plugins/AITool/services/test_interaction_coordinator.py`
- `python -m py_compile editor/plugins/AITool/cai_extensions/flows/model_retrieval_workflow/dispatch.py editor/plugins/AITool/cai_extensions/flows/model_retrieval_workflow/retrieve.py editor/plugins/AITool/cai_extensions/flows/model_retrieval_workflow/test_dispatch_image_retry.py`

### 待 F5/实机验证

- 下一轮 F5 中，若本地库已有灯笼、桌椅、导视牌等资产，日志应出现 `命中本地模型库，跳过图片补偿`，总耗时应明显下降。
- 对真正未缓存的新物体，如“天使雕像”，仍会进入单物体图片/模型生成；如果模型生成服务不可用，必须明确报不可用或降级原因，不能要求用户手动提供模型路径。
- 本修复不解决 embedding 401 本身；若图搜服务仍配置失败，未命中本地库的对象仍会降级生成。后续仍需单独修 embedding/provider 配置。

---

## 2026-06-18 追加修复：图搜/embedding 401 快速熔断与安全降级

### 已落地

- `model_retrieval_workflow.retrieve_node()` 新增图搜配置错误熔断：
  - 第一条真实图搜如果暴露 `401 / Invalid Token / API Key / 未配置` 等配置类错误，剩余对象不再并发重复调用图搜。
  - 剩余对象直接进入 `pending_generation`，并标记 `search_status=search_unavailable_fatal`。
  - 保留 `item_name / object_id / image_url / image_prompt`，确保后续模型生成仍可继续，不丢计划。
- `retrieve_single_item()` 对业务结果里的 `search_error` 做安全化：
  - 用户侧/最终报告只看到“图搜/embedding 配置不可用（凭据或服务配置错误），已跳过检索并转入生成。”
  - 原始异常只用于日志诊断，不再作为业务结果继续向后传播。

### 修复目标

- 对应 F5 里多次 embedding 401 导致检索逐物体慢失败的问题。
- 正常图搜仍保留；只有明确配置/凭据类错误才触发熔断。
- 与上一条“本地资产前置命中”组合后，资源链路顺序变为：
  1. 本地模型库命中：直接复用。
  2. 本地图片库命中：直接检索。
  3. 图搜可用：正常检索。
  4. 图搜配置不可用：快速熔断，转生成。
  5. 图片补偿/模型生成服务不可用：明确降级原因。

### 已自动验证

- `python editor/plugins/AITool/cai_extensions/flows/model_retrieval_workflow/test_retrieve_search_circuit.py`
- `python editor/plugins/AITool/cai_extensions/flows/model_retrieval_workflow/test_dispatch_image_retry.py`
- `python editor/plugins/AITool/cai_extensions/agent/test_scene_composer_progressive_geometry.py`
- `python editor/plugins/AITool/services/test_lanchat_agent_orchestrator.py`
- `python editor/plugins/AITool/services/test_generation_scheduler.py`
- `python editor/plugins/AITool/services/test_interaction_coordinator.py`
- `python -m py_compile editor/plugins/AITool/cai_extensions/flows/model_retrieval_workflow/retrieve.py editor/plugins/AITool/cai_extensions/flows/model_retrieval_workflow/test_retrieve_search_circuit.py`

### 待 F5/实机验证

- 如果下一轮仍存在 embedding 401，日志应只看到第一条真实图搜失败，后续对象应快速标记为 `search_unavailable_fatal`，不再每个物体都慢失败。
- 最终报告不应暴露 `Invalid Token / request id / api_key` 等内部错误细节。
- 若 embedding/provider 配置已修好，检索应正常运行，不应误触发熔断。

---

## 2026-06-18 追加修复：生成完成后的布局/边界修改进入最终调整闭环

### 已落地

- `InteractionCoordinator.ingest_message()` 修复完成态路由：
  - 主生成 `COMPLETED` 后，用户说“布局不合理、调整一下、摆放奇怪、动线不顺、栅栏奇怪、换成低矮藤蔓围栏”等修改类请求，不再创建新的 SeedPlan 草稿。
  - 这类请求会进入 `final_adjustment`，事件为 `final_adjustment_routed`，并纳入 `final_adjustment_plan()`。
  - 完成后的“添加/新增/再加一个”仍走 `post_generation_add` 追加批次。
- `LANChatAgentWorker` 增加完成态 @Agent 前置拦截：
  - `@长者 感觉布局不是很合理呀，调整一下` 不再调用角色模型闲聊。
  - worker 直接交给 Coordinator，返回“已记录为最终调整优先项”。
- 修复历史快照污染：
  - worker 在处理 @Agent trigger 前会同步聊天 history；如果当前 SeedPlan 已完成，history 中旧的普通讨论不再创建新 SeedPlan 覆盖 active plan。
  - 这解决了完成后修改被旧上下文顶掉、又回到 RoleAgent 闲聊的问题。
- 修复无意义 target 抽取：
  - “调整一下”不再把 `一下` 当成目标物体。
  - `_terrain_boundary / __terrain_boundary / 栅栏 / 边界` 仍规范到当前工具层实际使用的系统 actor `__terrain_boundary`，用于低风险地形边界调整。

### 修复目标

- 对应 F5 里“生成后引导修改不好用、AI 助手只是解释、不真正进入后续调整”的问题。
- 完成后修改应成为结构化最终调整输入，而不是普通聊天或新方案草稿。
- 对 `_terrain_boundary` 这类系统组件，用户用自然语言说“栅栏/边界”时，Coordinator 必须能定位到系统 actor，后续执行器才有机会做低风险修改。

### 已自动验证

- `python editor/plugins/AITool/services/test_interaction_coordinator.py`
- `python editor/plugins/AITool/services/test_lanchat_agent_orchestrator.py`
- `python -m py_compile editor/plugins/AITool/services/interaction_coordinator.py editor/plugins/AITool/services/lanchat_agent_worker.py editor/plugins/AITool/services/test_interaction_coordinator.py editor/plugins/AITool/services/test_lanchat_agent_orchestrator.py`

新增测试覆盖：

- 完成后 `@长者 感觉布局不是很合理呀，调整一下` 进入 `final_adjustment`，不新建 SeedPlan。
- 完成后 `@商人 这个栅栏有点奇怪，换成低矮藤蔓围栏` 定位到 `__terrain_boundary` 并进入 `final_adjustment`。
- worker 对完成后的布局调整不调用角色模型 agent，直接走 Coordinator。
- history snapshot 不会在完成态下用旧讨论覆盖 active plan。

### 待 F5/实机验证

- 下一轮 F5 完成生成后，说“布局不合理，调整一下”，日志应出现 `final_adjustment_routed`，而不是新的 SeedPlan draft 或 RoleAgent 普通聊天。
- 说“这个栅栏/边界有点奇怪，换成低矮藤蔓围栏”，pending intervention 应指向 `__terrain_boundary`。
- 这一轮只保证控制面闭环和最终调整计划输入；真正把 final adjustment plan 落成场景 delta 仍需在实机执行器阶段继续验证。

---

## 2026-06-18 追加修复：完成态 `_terrain_boundary` 低风险调整可执行

### 已落地

- `LANChatAgentWorker` 在完成态 `final_adjustment_routed` 后新增低风险执行桥：
  - 只处理明确指向真实 actor 或系统边界的请求。
  - `_terrain_boundary / __terrain_boundary / 栅栏 / 围栏 / 边界` 会规范定位到 `__terrain_boundary`。
  - 对“低矮、藤蔓、木栏、围栏、奇怪、不自然”等边界类请求，直接读取当前场景 actor 并做确定性低风险调整。
  - 当前支持边界高度压低到安全上限 `0.55`，并在工具可用时调整为自然木藤色。
- 对“整体布局不合理、调整一下”这类没有明确目标的请求，仍只进入 `final_adjustment_plan()`，不自动乱动场景。
- 该桥接优先使用 `corona_engine.get_scene_actors()`；实机没有该 helper 时，降级读取 `CoronaCore.core.managers.scene_manager`。
- 不调用角色模型，不要求用户提供模型路径，不新建 SeedPlan。

### 修复目标

- 对应 F5 中“用户明确说的是 `_terrain_boundary`，但助手找不到/误改入口拱门”的问题。
- 完成后的小修小补不再只停留在聊天解释层；至少对系统边界这类高频低风险对象，可以形成“Coordinator 记录 + 确定性执行 + 用户可见结果”的闭环。
- 保留安全边界：删除、高风险覆盖、全局重排仍不自动执行，应继续走 GM/最终调整计划。

### 已自动验证

- `python editor/plugins/AITool/services/test_lanchat_agent_orchestrator.py`
- `python editor/plugins/AITool/services/test_interaction_coordinator.py`
- `python editor/plugins/AITool/cai_extensions/agent/test_scene_session.py`
- `python -m py_compile editor/plugins/AITool/services/lanchat_agent_worker.py editor/plugins/AITool/services/test_lanchat_agent_orchestrator.py`

新增测试覆盖：

- 完成态 `@商人 ... _terrain_boundary ... 低矮木栏/藤蔓围栏` 不调用角色模型。
- worker 能从当前场景 actor 列表找到 `__terrain_boundary`。
- 边界 scale 的高度分量被压到 `0.55`，颜色被调整为自然木藤色。
- 请求仍进入 Coordinator 的 `final_adjustment`，不会绕过治理记录。

### 待 F5/实机验证

- 实机场景中 `__terrain_boundary` actor 是否暴露 `get_scale/set_scale/set_color` 或等价接口；若没有颜色接口，应至少完成高度调整并清楚说明材质替换不可用。
- 聊天里说“这个栅栏有点奇怪，换成低矮藤蔓围栏”后，界面应看到边界明显变矮或自然化，而不是误改入口拱门。
- 若请求是“整体布局不合理”，仍应只记录最终调整计划，不应无目标大范围移动物体。

---

## 2026-06-18 多人联调复盘：局域网模型同传卡顿与重复 Actor Create 广播

### 本轮现象

- 房主与同事进行局域网联调，导入 `armadillo.obj` 后，peer 端同传明显卡顿。
- 用户体感同时暴露：
  - 两层断链问题仍出现，但本轮运行未重新 F5 载入前一轮修改，不能作为新代码回归判断。
  - AI Agent 对聊天室/场景状态的视野仍不够稳定。
  - 希望后续补“批量添加 AI 助手模板”和“模板提示词优化”。

### 日志证据

- 本轮日志：`build/examples/engine/RelWithDebInfo/logs/2026-06-18_18-36-30_corona.log`
- `armadillo.obj` 加载阶段：
  - `20:15:57` 开始 MeshOpt，`Armadillo` 约 `1,037,423` unique vertices。
  - `20:16:16` 资源加载完成：`assets/model/armadillo.obj`。
  - `20:16:18` SceneTools 显示 actor 已加入场景。
- 文件同传阶段：
  - peer 请求 `Resource/armadillo.obj` 和 `Resource/armadillo.mtl` 各 1 次。
  - host 发送 `armadillo.obj`，大小约 `30,136,545` bytes，分 `58` 个 `512KB` chunk。
  - `armadillo.mtl` 仅 `247` bytes。
- 异常同步阶段：
  - 同一个 actor：`actor-2ada7659ea434c5cb3f4a6f2cb6a19c7`
  - `Broadcast actor create` 出现 `189` 次。
  - `Broadcast actor scene snapshot` 出现 `188` 次。
  - 时间从 `20:16:18` 持续到 `20:20:01`，约每 2 秒重复一次。
  - `20:20:01` peer 断开。

### 根因判断

- 主要卡顿不是 OBJ 单次加载本身，也不是文件请求无限重复：
  - 文件请求只有 2 次。
  - 但 actor create 和 scene snapshot 被周期性重复广播。
- 代码根因在前端 Network 面板：
  - `pollPeers()` 每 2 秒执行一次。
  - host 路径原来每轮都调用 `broadcastCurrentSceneSnapshot(currentSceneName.value, true)`。
  - 第二个参数 `true` 会把当前场景所有 actor create 一起重播。
  - 因此只要 peer 还在线，host 就会每 2 秒重发同一个 actor create，造成可靠通道、前端处理、远端导入/去重压力。
- 正确策略应是：
  - 周期性 host 心跳只发轻量 scene snapshot。
  - 只有 client 显式请求全量快照时，host 才携带 actor create，用于新加入 peer 补齐已有场景。

### 已落地修复

- `editor/Frontend/src/views/sidebar/Network.vue`
  - host 周期轮询从：
    - `broadcastCurrentSceneSnapshot(currentSceneName.value, true)`
  - 改为：
    - `broadcastCurrentSceneSnapshot(currentSceneName.value, false)`
  - 收到 client `ACTOR_SCENE_SNAPSHOT_REQUEST` 时仍保留：
    - `broadcastCurrentSceneSnapshot(sceneName, true)`
- `editor/plugins/AITool/services/test_native_lanchat_bridge_static.py`
  - 新增静态守护：
    - host polling 不允许每 2 秒重播 actor create。
    - client 显式请求全量快照时仍必须保留 actor create。

### 已自动验证

- `python editor/plugins/AITool/services/test_native_lanchat_bridge_static.py`
- `python -m py_compile editor/plugins/AITool/services/test_native_lanchat_bridge_static.py`

### 验证缺口

- `npm run lint` 在 PowerShell 下被 `npm.ps1` 执行策略拦截。
- 改用 `npm.cmd run lint -- --quiet` 后，失败原因为 `eslint` 不可用，推测 `editor/Frontend/node_modules` 未安装或当前前端依赖环境未准备好。
- 未做 native build / CEF 实机编译，符合本轮边界。

### 待 F5/实机验证

- 重新 F5 载入本轮修改后，再进行同样的双人局域网联调。
- 导入同一个大模型时，日志应满足：
  - `FILE_REQUEST` 仍只按缺失资源请求。
  - `Broadcast actor create` 对同一 actor 不应每 2 秒重复出现。
  - 周期性 `Broadcast actor scene snapshot` 可保留轻量心跳，但不应携带 actor create。
- 若仍卡顿，下一步重点看：
  - 大 OBJ 首次解析/LOD 切分是否阻塞主线程。
  - `FILE_CHUNK` 是否一次性 58 个可靠包全部塞入 ENet 队列，需要限速/背压。
  - 远端 create actor 后是否重复导入或未注册 identity。

### 后续小功能记录

- 批量添加 AI 助手模板：
  - 支持一次选择多个内置模板加入房间，例如长者/商人/山贼/小女孩。
  - 批量加入时应由房主确认，避免普通成员误触发。
- 模板提示词优化：
  - 每个 Agent 模板应明确“聊天室视野边界”：能看到哪些讨论摘要、当前 SeedPlan、生成状态、已吸收/待处理介入。
  - 模板不应假装知道未暴露的内部工具状态。
  - Agent 回答“现在情况如何”时，应优先引用 Coordinator/Disclosure 的结构化状态，而不是凭聊天历史猜测。
---

## 2026-06-18 晚间验证前补丁：单人/多人稳定性收口

### 本轮修复目标

- 面向今晚单人、多人 F5 验证，优先处理会直接阻塞链路或造成误判的问题。
- 暂缓“完成态整体布局不合理 -> 自动生成布局重排 delta”这一类高风险执行策略，只保留记录和最终调整计划入口。

### 已落地修复

- `LANChatAgentWorker._prepare_confirmed_action_payload()` 增加 SeedPlan 确认失败保护：
  - 当 Coordinator 因未澄清、冲突未解决、房主身份缺失等原因拒绝确认时，不再直接读取 `confirmed.payload["plan_id"]`。
  - 该场景会降级为 `discussion_only / coordinator_confirmation_blocked`，不会广播生成动作，也不会进入 host executor。
  - 对应最近日志中 `KeyError: 'plan_id'` 风险。
- `InteractionCoordinator._is_status_query()` 扩大状态查询识别：
  - 覆盖“现在情况是什么样”“生成计划是什么”“为什么执行生成计划”“现在生成到哪里”等真实聊天表达。
  - 目标是让状态询问只读 Coordinator 状态，不生成 GM 提案，不误入 SeedPlan。
- `RoleTemplate.inject()` 增加 Agent 视野边界：
  - Agent 只能基于可见聊天、系统进度和工具返回作答。
  - 不确定生成进度、模型导入或场景物体状态时，要说明不确定并建议查询 GM/系统状态。
  - 不允许凭角色口吻臆造执行结果。
- `RoomPanel.vue` 增加“夜市验证组”快捷入口：
  - 一键添加长者、商人、小女孩、山贼。
  - 仅复用现有 `lanchat.addAgent()` 协议，不新增 native/后端接口。
- 多人模型同传卡顿前一节已修复：
  - host 周期轮询不再每 2 秒重复广播 actor create。
  - client 显式请求全量快照时仍保留 actor create 补齐。

### 已自动验证

- `python editor/plugins/AITool/services/test_lanchat_agent_orchestrator.py`
- `python editor/plugins/AITool/services/test_interaction_coordinator.py`
- `python editor/plugins/AITool/services/test_native_lanchat_bridge_static.py`
- `python editor/plugins/AITool/cai_extensions/agent/test_role_registry.py`
- `python -m py_compile editor/plugins/AITool/services/lanchat_agent_worker.py editor/plugins/AITool/services/interaction_coordinator.py editor/plugins/AITool/services/test_lanchat_agent_orchestrator.py`
- `python -m py_compile editor/plugins/AITool/cai_extensions/agent/role_registry.py editor/plugins/AITool/cai_extensions/agent/test_role_registry.py editor/plugins/AITool/services/test_native_lanchat_bridge_static.py`

### 验证缺口

- 前端完整 lint 未跑通：
  - `npm run lint` 在 PowerShell 下会被 `npm.ps1` 执行策略阻止。
  - `npm.cmd run lint -- --quiet` 仍因 `eslint` 不可用失败，推测前端依赖未安装或环境未准备。
- 未做 CMake/Ninja/native build，符合本轮边界。

### 晚间 F5 验证重点

- 单人验证：
  - 直接用一个助手完成“方案讨论 -> 确认开始 -> 分批生成 -> 中途新增天使雕像/小狗 -> 完成后查询状态”。
  - 重点看状态查询是否只读、生成是否进入队列、追加生成是否不再要求用户提供模型路径。
- 多人验证：
  - 房主加“夜市验证组”，同事作为普通用户加入。
  - 复测大模型/OBJ 同传：同一 actor 不应每 2 秒重复 `Broadcast actor create`。
  - 普通用户和房主分别询问“现在情况是什么样/生成计划是什么”，不应触发 GM 提案或新的生成任务。
- 仍需人工观察：
  - 真实前端按钮布局是否正常。
  - 角色回复是否明显减少“我猜已经执行了”的错觉。
  - 完成态无明确 actor 的“布局不合理”仍只应进入记录/最终调整计划，不应乱动场景。

---

## 2026-06-18 追加修复：VLM 前端可选开关

### 已落地

- `RoomPanel.vue` 房主端新增 `VLM 外观检查` 开关：
  - 默认关闭。
  - 打开后本轮房主消息会携带 `generation_options.vlm_enabled=true / vlm_max_targets=1`。
  - 关闭后携带 `vlm_max_targets=0`。
- `lanchat.js` 新增房间内生成选项状态：
  - `generationOptions.vlmEnabled`
  - `generationOptions.vlmMaxTargets`
  - `generationOptionsMetadata()`
- `LANChatAgentWorker` 读取房主消息 metadata：
  - 只接受房主消息或 host metadata。
  - 将 `PROGRESSIVE_VLM_MAX_TARGETS` 更新为 `0..4`。
  - 普通参与者消息不会改变房主机器上的 VLM 设置。

### 使用策略

- 普通验证/多人联调默认关闭 VLM，避免截图和审查拖慢主链路。
- 需要观察 VLM 时由房主打开开关，当前先只审查 1 个关键目标。
- VLM 仍是 advisory 外回路：只生成检查建议和最终报告，不直接改场景。

### 已自动验证

- `python editor/plugins/AITool/services/test_lanchat_agent_orchestrator.py`
- `python editor/plugins/AITool/services/test_native_lanchat_bridge_static.py`
- `python -m py_compile editor/plugins/AITool/services/lanchat_agent_worker.py editor/plugins/AITool/services/test_lanchat_agent_orchestrator.py editor/plugins/AITool/services/test_native_lanchat_bridge_static.py`

### 待 F5/实机验证

- 房主打开开关后生成，最终报告应出现真实 `VLM/外观检查` 或明确截图/服务不可用原因。
- 房主关闭开关后生成，最终报告应显示 VLM 未执行或不触发 VLM 审查。
- 普通用户发消息不应改变 VLM 开关状态。

---

## 2026-06-18 晚间验收前审查核验：非 native 门禁与剩余硬缺口

### 审查结论

今天的非 native 验收门禁已经恢复通过，可以进入晚间 F5/实机验证；但“完整大分批资源闭环、VLM 批次审查、布局不合理自动重排 delta”不应作为今晚硬验收门槛，已记录到 `docs/终极计划.md` 第 11 章作为后续待办。

### 已自动核验通过

- `python editor/plugins/AITool/services/verify_ultimate_plan.py`
  - 结果：`[SUMMARY] All non-native ultimate-plan checks passed.`
- `python editor/plugins/AITool/cai_extensions/agent/test_scene_composer_progressive_geometry.py`
  - 覆盖 progressive micro-batch、pending resource request、显式图片阶段、VLM target priority、terrain boundary profile 等。
- `python editor/plugins/AITool/services/test_lanchat_agent_orchestrator.py`
  - 覆盖 SeedPlan 确认、状态查询只读、VLM host-only metadata、完成态布局调整路由、`_terrain_boundary` 低风险调整等。
- `node editor/Frontend/scripts/test-lanchat-roster.mjs`
  - 覆盖 LANChat roster、夜市验证组、GM 结构化消息、前端静态契约。

说明：测试输出中的 401、queue full、VLM timeout、broken import 等日志为故障注入用例，最终均由测试断言吸收，不代表本次门禁失败。

### 本轮发现并修复的门禁问题

- 初次总门禁失败点：
  - `editor/Frontend/scripts/test-lanchat-roster.mjs`
  - 失败原因：`RoomPanel.vue` 为 `@GM` 消息合并 VLM metadata 后，静态门禁未看到 `return buildManualGmMessageOptions(s.role)` 这个共享 builder 路径。
- 修复：
  - `RoomPanel.vue` 保留 `@GM` 手动消息的共享 builder 路径。
  - 仅在需要携带 host-only generation options 时合并 `generation_options` metadata。
- 复验：
  - `node editor/Frontend/scripts/test-lanchat-roster.mjs` 通过。
  - `python editor/plugins/AITool/services/verify_ultimate_plan.py` 通过。

### 今晚 F5 应重点验收

- 单人链路：
  - 讨论 -> 方案确认 -> SeedPlan -> 生成队列 -> 分批导入 -> 中途新增对象 -> 完成后状态查询。
  - 重点观察“状态查询”是否仍会误生成 GM proposal 或 SeedPlan。
- 多人链路：
  - 房主与普通用户同时发言时，普通聊天应进入 Coordinator 记忆/介入，不应直接触发生成。
  - 房主确认仍应是生成执行的唯一入口。
- 动态介入：
  - 生成中“新增天使雕像/小狗”应进入后续批次或明确失败原因。
  - 完成后“添加生成一个 X”应走追加批次，不应要求用户提供模型路径。
- 地形与边界：
  - `_terrain_boundary / 栅栏 / 边界` 应定位到 `__terrain_boundary`。
  - 低风险边界调整应能执行；大范围地形重排仍只记录为待确认/待实现。
- VLM：
  - 默认关闭时不拖慢主链路。
  - 房主打开后，最终报告应明确 `VLM/外观检查` 是否执行、审查对象、建议或失败原因。
  - 当前 VLM 仍是最终外审为主，不是批次边界硬检查点。
- 多人局域网同步：
  - 大模型同传时不应再每 2 秒重复 `Broadcast actor create` 同一 actor。

### 今晚不建议硬卡的项目

- 完整“图片生成 -> 模型生成 -> 导入”的初始资产大分批闭环。
- 第一批/中间批/最终批的分层 VLM checkpoint。
- “布局不合理”自动生成可执行全局重排 delta。

这三项属于架构质量提升项，已经进入终极计划待办。今晚若实机暴露相关问题，应记录证据，但不建议因此否定当前控制面和基础生成链路。

---

## 2026-06-18 晚间 F5 复盘补充：生成进度信息披露断链

### 现象

- 最新运行已进入 `GenerationScheduler` 和 `SceneComposer` 后台生成链路，但聊天室没有稳定展示“理解方案 / 资源准备 / 分批组装 / 最终检查”等进度。
- 日志中只有 `GenerationScheduler status_change`、`SceneComposer`、`model_retrieval workflow` 和 `3D generation batch` 底层日志，用户侧几乎看不到对应进度。
- 这和 CEF `render_input_router.cc` DCHECK 崩溃无直接因果关系；CEF 崩溃是 UI 容器干扰，披露缺失是 Python/LANChat 进度转发链路问题。

### 根因

- `SceneComposer.compose()` 本身会通过 `progress_sink` 输出 5% / 18% / 32% 等用户可理解进度。
- 但本轮生成由 `GenerationScheduler` 后台线程执行，未稳定注入 LANChat room-safe `progress_sink`。
- 因此进度只留在内部日志或 composer 回调链路，没有变成 `network_send_system_message_ex(..., message_kind="action_status")`。

### 已修复

- `LANChatAgentWorker` 在安装 scheduler hook 时，为 `scene_generation*` job 自动注入 `progress_sink`。
- 该 sink 会把后台生成进度转成安全的房间系统消息：`理解方案` / `资源准备` / `分批组装` / `最终检查` / `生成中`。
- 披露 metadata 只保留 `room_id / plan_id / stage / progress / source` 等安全字段，不暴露 `job_id / session_id / prompt / worker`。
- 对同房间相同文本做 1 秒去重，避免生成阶段刷屏。

### 已自动验证

- `python editor/plugins/AITool/services/test_lanchat_agent_orchestrator.py`
  - 新增覆盖：后台 composer 调用 `progress_sink("生成进度 32% ... 准备所需模型")` 后，LANChat 系统消息可见。
  - 断言 `stage="资源准备"`、`progress=32`，且不泄露 `prompt/session_id`。
- `python -m py_compile editor/plugins/AITool/services/lanchat_agent_worker.py editor/plugins/AITool/services/test_lanchat_agent_orchestrator.py`

### 待 F5/实机验证

- 重新 F5 后，生成过程中聊天室应至少看到：`生成进度 5%` 理解方案、`生成进度 18%` 空间拆分、`生成进度 32%` 资源准备、后续分批导入/放入进度。
- 生成很慢时，用户不应再感觉“完全没动静”。
- 若仍无展示，下一步检查 native `network_send_system_message_ex` 到前端 action_status 渲染链路。
