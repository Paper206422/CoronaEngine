# CAI API 参考入口

CAI 公共 API 参考由 `Quasar` submodule 维护，内容包括 `CAIApp`、`CAIRuntime`、`ChatRequest`、`StreamEvent` 与 plugin API。

该文档保留在 CAI submodule 内，便于 CAI 独立发布时携带自身 API 说明。CoronaEngine 侧只维护入口说明，避免在根文档中复制一份容易漂移的 API 参考。

源码位置：`editor/plugins/AITool/Quasar/` 下的 API 参考文档。